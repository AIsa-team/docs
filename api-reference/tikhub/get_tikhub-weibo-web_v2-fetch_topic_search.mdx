---
title: "Weibo topic search"
description: "Weibo topic search"
openapi: "openapi/tikhub.json GET /tikhub/weibo/web_v2/fetch_topic_search"
---

Weibo topic search

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/weibo/web_v2/fetch_topic_search?query=yu7" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
