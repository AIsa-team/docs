---
title: "Get hot search data"
description: "获取热门搜索信息/Get hot search data"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/web/fetch_hot_search"
---

获取热门搜索信息/Get hot search data

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/web/fetch_hot_search?limit=10" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
