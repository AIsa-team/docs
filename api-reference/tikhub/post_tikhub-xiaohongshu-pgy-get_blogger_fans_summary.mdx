---
title: "Get PGY blogger fans summary"
description: "获取蒲公英博主粉丝概览/Get PGY blogger fans summary"
openapi: "openapi/tikhub.json POST /tikhub/xiaohongshu/pgy/get_blogger_fans_summary"
---

获取蒲公英博主粉丝概览/Get PGY blogger fans summary

**Pricing** — base $0.029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/pgy/get_blogger_fans_summary" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
