---
title: "Get single user data V2"
description: "Get single user data V2"
openapi: "openapi/tikhub-en.json GET /tikhub/kuaishou/app/fetch_one_user_v2"
---

Get single user data V2

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/kuaishou/app/fetch_one_user_v2?user_id=3xz63mn6fngqtiq" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
