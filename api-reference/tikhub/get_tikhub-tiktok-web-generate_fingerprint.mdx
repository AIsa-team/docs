---
title: "Generate browser fingerprint"
description: "Generate browser fingerprint"
openapi: "openapi/tikhub-en.json GET /tikhub/tiktok/web/generate_fingerprint"
---

Generate browser fingerprint

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/web/generate_fingerprint" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
