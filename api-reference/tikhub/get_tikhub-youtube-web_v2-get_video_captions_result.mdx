---
title: "Get video captions async job result"
description: "Get video captions async job result"
openapi: "openapi/tikhub-en.json GET /tikhub/youtube/web_v2/get_video_captions_result"
---

Get video captions async job result

**Pricing** — base $0.0001 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/youtube/web_v2/get_video_captions_result?job_id=123e4567-e89b-12d3-a456-426614174000" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
