---
title: "Get daren top videos"
description: "获取达人视频/Get daren top videos"
openapi: "openapi/tikhub.json POST /tikhub/douyin/index/fetch_daren_great_user_top_video"
---

获取达人视频/Get daren top videos

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/douyin/index/fetch_daren_great_user_top_video" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'
```
