---
title: "Get trending hashtag detail"
description: "获取热门标签详情(趋势)/Get trending hashtag detail"
openapi: "openapi/tikhub.json POST /tikhub/tiktok/ads/get_trends_hashtag_detail"
---

获取热门标签详情(趋势)/Get trending hashtag detail

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/tiktok/ads/get_trends_hashtag_detail" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
