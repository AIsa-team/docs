---
title: "Search image"
description: "搜索图片作品/Search image"
openapi: "openapi/tikhub.json GET /tikhub/kuaishou/app/search_image"
---

搜索图片作品/Search image

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/kuaishou/app/search_image?keyword=风景" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
