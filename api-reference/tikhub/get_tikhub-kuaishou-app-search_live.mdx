---
title: "Search live"
description: "搜索直播间/Search live"
openapi: "openapi/tikhub.json GET /tikhub/kuaishou/app/search_live"
---

搜索直播间/Search live

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/kuaishou/app/search_live?keyword=带货" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
