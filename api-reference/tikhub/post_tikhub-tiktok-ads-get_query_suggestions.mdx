---
title: "Get query suggestions"
description: "Get query suggestions"
openapi: "openapi/tikhub.json POST /tikhub/tiktok/ads/get_query_suggestions"
---

Get query suggestions

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/tiktok/ads/get_query_suggestions" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
