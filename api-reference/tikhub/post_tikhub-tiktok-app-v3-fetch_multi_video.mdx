---
title: "Batch Get Video Information"
description: "批量获取视频信息/Batch Get Video Information"
openapi: "openapi/tikhub.json POST /tikhub/tiktok/app/v3/fetch_multi_video"
---

批量获取视频信息/Batch Get Video Information

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/tiktok/app/v3/fetch_multi_video" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
