---
title: "Fetch hashtag suggestions"
description: "获取话题推荐搜索/Fetch hashtag suggestions"
openapi: "openapi/tikhub.json POST /tikhub/douyin/search/fetch_challenge_suggest"
---

获取话题推荐搜索/Fetch hashtag suggestions

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/douyin/search/fetch_challenge_suggest" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
