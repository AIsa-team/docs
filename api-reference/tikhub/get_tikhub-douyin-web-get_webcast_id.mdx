---
title: "Extract webcast id"
description: "Extract webcast id"
openapi: "openapi/tikhub.json GET /tikhub/douyin/web/get_webcast_id"
---

Extract webcast id

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/web/get_webcast_id?url=https://live.douyin.com/775841227732" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
