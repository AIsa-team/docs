---
title: "Search Huahuo creators or content"
description: "UP主广场搜索（搜UP主 / 搜内容）/Search Huahuo creators or content"
openapi: "openapi/tikhub.json POST /tikhub/bilibili/huahuo/search_upper"
---

UP主广场搜索（搜UP主 / 搜内容）/Search Huahuo creators or content

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/bilibili/huahuo/search_upper" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
