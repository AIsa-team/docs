---
title: "Get user recommend timeline"
description: "获取微博主页推荐时间轴/Get user recommend timeline"
openapi: "openapi/tikhub.json GET /tikhub/weibo/web_v2/fetch_user_recommend_timeline"
---

获取微博主页推荐时间轴/Get user recommend timeline

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/weibo/web_v2/fetch_user_recommend_timeline" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
