---
title: "Get IP Activity Detail"
description: "获取星图IP活动详情/Get IP Activity Detail"
openapi: "openapi/tikhub.json GET /tikhub/douyin/xingtu_v2/get_ip_activity_detail"
---

获取星图IP活动详情/Get IP Activity Detail

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/xingtu_v2/get_ip_activity_detail?id=347" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
