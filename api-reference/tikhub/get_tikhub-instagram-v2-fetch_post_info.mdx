---
title: "Get post info"
description: "Get post info"
openapi: "openapi/tikhub-en.json GET /tikhub/instagram/v2/fetch_post_info"
---

Get post info

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/instagram/v2/fetch_post_info?code_or_url=DRhvwVLAHAG" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
