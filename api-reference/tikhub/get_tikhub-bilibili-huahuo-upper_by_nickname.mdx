---
title: "Find creators by nickname"
description: "按昵称查UP主/Find creators by nickname"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/huahuo/upper_by_nickname"
---

按昵称查UP主/Find creators by nickname

**Pricing** — base $0.0087 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/huahuo/upper_by_nickname?nickname=VALUE" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
