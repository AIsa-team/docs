---
title: "Get user's posts"
description: "Get user's posts"
openapi: "openapi/tikhub-en.json GET /tikhub/linkedin/web_v2/get_user_posts"
---

Get user's posts

**Pricing** — base $0.0725 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/linkedin/web_v2/get_user_posts?url=https://www.linkedin.com/in/williamhgates/" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
