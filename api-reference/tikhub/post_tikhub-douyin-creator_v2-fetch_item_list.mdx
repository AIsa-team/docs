---
title: "Fetch item list"
description: "获取投稿作品列表/Fetch item list"
openapi: "openapi/tikhub.json POST /tikhub/douyin/creator_v2/fetch_item_list"
---

获取投稿作品列表/Fetch item list

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/douyin/creator_v2/fetch_item_list" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
