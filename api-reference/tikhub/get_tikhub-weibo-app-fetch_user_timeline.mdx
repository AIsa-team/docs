---
title: "Get user timeline"
description: "获取用户发布的微博/Get user timeline"
openapi: "openapi/tikhub.json GET /tikhub/weibo/app/fetch_user_timeline"
---

获取用户发布的微博/Get user timeline

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/weibo/app/fetch_user_timeline?uid=7648703289" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
