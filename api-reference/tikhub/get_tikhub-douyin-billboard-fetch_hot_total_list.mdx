---
title: "Fetch total hot list"
description: "Fetch total hot list"
openapi: "openapi/tikhub-en.json GET /tikhub/douyin/billboard/fetch_hot_total_list"
---

Fetch total hot list

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/billboard/fetch_hot_total_list?page=1&page_size=10&type=VALUE" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
