---
title: "Get topic feed"
description: "Get topic feed"
openapi: "openapi/tikhub-en.json GET /tikhub/xiaohongshu/app_v2/get_topic_feed"
---

Get topic feed

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/app_v2/get_topic_feed?page_id=5c1cc866febed9000184b7c1" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
