---
title: "Fetch user search V2"
description: "获取用户搜索 V2/Fetch user search V2"
openapi: "openapi/tikhub.json POST /tikhub/douyin/search/fetch_user_search_v2"
---

获取用户搜索 V2/Fetch user search V2

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/douyin/search/fetch_user_search_v2" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
