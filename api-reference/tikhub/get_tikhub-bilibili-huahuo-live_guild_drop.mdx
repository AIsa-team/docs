---
title: "Get live-streaming guilds"
description: "直播公会下拉/Get live-streaming guilds"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/huahuo/live_guild_drop"
---

直播公会下拉/Get live-streaming guilds

**Pricing** — base $0.0087 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/huahuo/live_guild_drop" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
