---
title: "Fetch single video by URL"
description: "Fetch single video by URL"
openapi: "openapi/tikhub-en.json GET /tikhub/kuaishou/web/fetch_one_video_by_url"
---

Fetch single video by URL

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/kuaishou/web/fetch_one_video_by_url?url=https://v.kuaishou.com/GKTpYm" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
