---
title: "Fetch hot challenge list"
description: "获取挑战热榜/Fetch hot challenge list"
openapi: "openapi/tikhub.json GET /tikhub/douyin/billboard/fetch_hot_challenge_list"
---

获取挑战热榜/Fetch hot challenge list

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/billboard/fetch_hot_challenge_list?page=1&page_size=10" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
