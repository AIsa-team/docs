---
title: "Get home recommend feed"
description: "Get home recommend feed"
openapi: "openapi/tikhub-en.json GET /tikhub/weibo/app/fetch_home_recommend_feed"
---

Get home recommend feed

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/weibo/app/fetch_home_recommend_feed" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
