---
title: "Get live daily rank data"
description: "获取直播每日榜单数据/Get live daily rank data"
openapi: "openapi/tikhub.json GET /tikhub/tiktok/app/v3/fetch_live_daily_rank"
---

获取直播每日榜单数据/Get live daily rank data

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/app/v3/fetch_live_daily_rank" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
