---
title: "Get daily trending search words"
description: "获取每日趋势搜索关键词/Get daily trending search words"
openapi: "openapi/tikhub.json GET /tikhub/tiktok/web/fetch_trending_searchwords"
---

获取每日趋势搜索关键词/Get daily trending search words

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/web/fetch_trending_searchwords" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
