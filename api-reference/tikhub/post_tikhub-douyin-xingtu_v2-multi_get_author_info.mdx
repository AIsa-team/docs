---
title: "Multi Get Author Info"
description: "批量获取创作者信息/Multi Get Author Info"
openapi: "openapi/tikhub.json POST /tikhub/douyin/xingtu_v2/multi_get_author_info"
---

批量获取创作者信息/Multi Get Author Info

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/douyin/xingtu_v2/multi_get_author_info" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
