---
title: "Fetch user live replay"
description: "Fetch user live replay"
openapi: "openapi/tikhub.json GET /tikhub/kuaishou/web/fetch_user_live_replay"
---

Fetch user live replay

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/kuaishou/web/fetch_user_live_replay?user_id=3xz63mn6fngqtiq" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
