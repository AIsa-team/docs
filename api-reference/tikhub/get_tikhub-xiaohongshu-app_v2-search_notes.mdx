---
title: "Search notes"
description: "Search notes"
openapi: "openapi/tikhub.json GET /tikhub/xiaohongshu/app_v2/search_notes"
---

Search notes

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/app_v2/search_notes?keyword=food recommendations" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
