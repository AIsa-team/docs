---
title: "Fetch user info"
description: "获取用户信息/Fetch user info"
openapi: "openapi/tikhub.json GET /tikhub/xiaohongshu/web_v3/fetch_user_info"
---

获取用户信息/Fetch user info

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/web_v3/fetch_user_info?user_id=5e3a8ee700000000010070c6" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
