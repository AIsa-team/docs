---
title: "Extract user sec_user_id"
description: "Extract user sec_user_id"
openapi: "openapi/tikhub.json GET /tikhub/tiktok/web/get_sec_user_id"
---

Extract user sec_user_id

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/web/get_sec_user_id?url=https://www.tiktok.com/@tiktok" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
