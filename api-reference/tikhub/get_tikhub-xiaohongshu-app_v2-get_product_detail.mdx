---
title: "Get product detail"
description: "Get product detail"
openapi: "openapi/tikhub-en.json GET /tikhub/xiaohongshu/app_v2/get_product_detail"
---

Get product detail

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/app_v2/get_product_detail?sku_id=669ddd44e05f3700011067ed" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
