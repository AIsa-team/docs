---
title: "Get Item Report Comments"
description: "Get Item Report Comments"
openapi: "openapi/tikhub-en.json POST /tikhub/douyin/xingtu_v2/get_item_report_comments"
---

Get Item Report Comments

**Pricing** — base $0.00435 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/douyin/xingtu_v2/get_item_report_comments" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
