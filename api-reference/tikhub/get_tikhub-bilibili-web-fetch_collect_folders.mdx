---
title: "Get user collection folders"
description: "Get user collection folders"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/web/fetch_collect_folders"
---

Get user collection folders

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/web/fetch_collect_folders?uid=178360345" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
