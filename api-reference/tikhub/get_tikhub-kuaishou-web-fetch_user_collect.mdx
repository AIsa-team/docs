---
title: "Fetch user collect"
description: "获取用户收藏作品/Fetch user collect"
openapi: "openapi/tikhub.json GET /tikhub/kuaishou/web/fetch_user_collect"
---

获取用户收藏作品/Fetch user collect

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/kuaishou/web/fetch_user_collect?user_id=3xz63mn6fngqtiq" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
