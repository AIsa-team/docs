---
title: "Get image note detail"
description: "Get image note detail"
openapi: "openapi/tikhub.json GET /tikhub/xiaohongshu/app_v2/get_image_note_detail"
---

Get image note detail

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/app_v2/get_image_note_detail" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
