---
title: "Get music posts"
description: "Get music posts"
openapi: "openapi/tikhub-en.json GET /tikhub/instagram/v2/fetch_music_posts"
---

Get music posts

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/instagram/v2/fetch_music_posts?audio_canonical_id=564058920086577" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
