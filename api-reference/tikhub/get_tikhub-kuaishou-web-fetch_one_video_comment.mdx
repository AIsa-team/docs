---
title: "Fetch video comments"
description: "Fetch video comments"
openapi: "openapi/tikhub-en.json GET /tikhub/kuaishou/web/fetch_one_video_comment"
---

Fetch video comments

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/kuaishou/web/fetch_one_video_comment?photo_id=3x7gxp2zhgjv832" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
