---
title: "Content publish trend"
description: "Content publish trend"
openapi: "openapi/tikhub.json GET /tikhub/douyin/index/fetch_content_publish_trend"
---

Content publish trend

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/index/fetch_content_publish_trend?tag_id=VALUE&start_date=VALUE&end_date=VALUE" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
