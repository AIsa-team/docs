---
title: "Get Zhihu Preset Search"
description: "Get Zhihu Preset Search"
openapi: "openapi/tikhub.json GET /tikhub/zhihu/web/fetch_preset_search"
---

Get Zhihu Preset Search

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/zhihu/web/fetch_preset_search" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
