---
title: "Generate ttwid"
description: "生成ttwid/Generate ttwid"
openapi: "openapi/tikhub.json GET /tikhub/tiktok/web/generate_ttwid"
---

生成ttwid/Generate ttwid

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/web/generate_ttwid" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
