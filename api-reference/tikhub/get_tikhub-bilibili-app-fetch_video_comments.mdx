---
title: "Get video comments"
description: "获取视频评论列表/Get video comments"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/app/fetch_video_comments"
---

获取视频评论列表/Get video comments

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/app/fetch_video_comments" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
