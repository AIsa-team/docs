---
title: "Generate XGnarly and XBogus"
description: "生成 XGnarly 和 XBogus /Generate XGnarly and XBogus"
openapi: "openapi/tikhub.json POST /tikhub/tiktok/web/generate_xgnarly_and_xbogus"
---

生成 XGnarly 和 XBogus /Generate XGnarly and XBogus

**Pricing** — base $0.00725 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/tiktok/web/generate_xgnarly_and_xbogus" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
