---
title: "Get post detail"
description: "Get post detail"
openapi: "openapi/tikhub-en.json GET /tikhub/threads/web/fetch_post_detail"
---

Get post detail

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/threads/web/fetch_post_detail?post_id=3349029093483693129" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
