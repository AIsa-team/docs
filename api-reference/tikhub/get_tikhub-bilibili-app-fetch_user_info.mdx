---
title: "Get user info"
description: "Get user info"
openapi: "openapi/tikhub-en.json GET /tikhub/bilibili/app/fetch_user_info"
---

Get user info

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/app/fetch_user_info?user_id=203680252" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
