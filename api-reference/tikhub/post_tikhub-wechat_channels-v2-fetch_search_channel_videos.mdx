---
title: "Search Videos Within a WeChat Channel"
description: "Search Videos Within a WeChat Channel"
openapi: "openapi/tikhub-en.json POST /tikhub/wechat_channels/v2/fetch_search_channel_videos"
---

Search Videos Within a WeChat Channel

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/wechat_channels/v2/fetch_search_channel_videos" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
