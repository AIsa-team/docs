---
title: "Fetch user info"
description: "获取用户信息/Fetch user info"
openapi: "openapi/tikhub.json GET /tikhub/kuaishou/web/fetch_user_info"
---

获取用户信息/Fetch user info

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/kuaishou/web/fetch_user_info?user_id=3xz63mn6fngqtiq" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
