---
title: "AI Smart Search"
description: "AI Smart Search"
openapi: "openapi/tikhub.json GET /tikhub/weibo/app/fetch_ai_smart_search"
---

AI Smart Search

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/weibo/app/fetch_ai_smart_search?query=artificial intelligence" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
