---
title: "Get live-streaming areas"
description: "直播分区下拉/Get live-streaming areas"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/huahuo/live_area_list"
---

直播分区下拉/Get live-streaming areas

**Pricing** — base $0.0087 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/huahuo/live_area_list" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
