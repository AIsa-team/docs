---
title: "Content author portrait"
description: "创作者画像/Content author portrait"
openapi: "openapi/tikhub.json POST /tikhub/douyin/index/fetch_content_author_portrait"
---

创作者画像/Content author portrait

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/douyin/index/fetch_content_author_portrait" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'
```
