---
title: "Batch channel info"
description: "Batch channel info"
openapi: "openapi/tikhub-en.json GET /tikhub/telegram/web/batch_channel_info"
---

Batch channel info

**Pricing** — base $0.0116 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/telegram/web/batch_channel_info?channels=durov,telegram" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
