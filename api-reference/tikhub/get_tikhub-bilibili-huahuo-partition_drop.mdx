---
title: "Get video partition options"
description: "稿件分区下拉/Get video partition options"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/huahuo/partition_drop"
---

稿件分区下拉/Get video partition options

**Pricing** — base $0.0087 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/huahuo/partition_drop" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
