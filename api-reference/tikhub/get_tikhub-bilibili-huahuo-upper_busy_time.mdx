---
title: "Get creator unavailable dates"
description: "Get creator unavailable dates"
openapi: "openapi/tikhub-en.json GET /tikhub/bilibili/huahuo/upper_busy_time"
---

Get creator unavailable dates

**Pricing** — base $0.0087 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/huahuo/upper_busy_time?mid=1" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
