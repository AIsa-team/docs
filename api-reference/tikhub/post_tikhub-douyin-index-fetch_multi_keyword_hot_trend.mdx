---
title: "Get multi-keyword hot trend"
description: "获取多关键词热度趋势/Get multi-keyword hot trend"
openapi: "openapi/tikhub.json POST /tikhub/douyin/index/fetch_multi_keyword_hot_trend"
---

获取多关键词热度趋势/Get multi-keyword hot trend

**Pricing** — base $0.00435 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/douyin/index/fetch_multi_keyword_hot_trend" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'
```
