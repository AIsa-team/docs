---
title: "Register device for TikTok Web"
description: "Register device for TikTok Web"
openapi: "openapi/tikhub-en.json GET /tikhub/tiktok/web/device_register"
---

Register device for TikTok Web

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/web/device_register" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
