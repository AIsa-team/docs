---
title: "Get PGY note detail"
description: "Get PGY note detail"
openapi: "openapi/tikhub-en.json POST /tikhub/xiaohongshu/pgy/get_note_detail"
---

Get PGY note detail

**Pricing** — base $0.029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/pgy/get_note_detail" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
