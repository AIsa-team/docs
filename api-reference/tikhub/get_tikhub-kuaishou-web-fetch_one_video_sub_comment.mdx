---
title: "Fetch video sub comments"
description: "Fetch video sub comments"
openapi: "openapi/tikhub-en.json GET /tikhub/kuaishou/web/fetch_one_video_sub_comment"
---

Fetch video sub comments

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/kuaishou/web/fetch_one_video_sub_comment?photo_id=3xgarycnydawq3g&root_comment_id=908850553827" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
