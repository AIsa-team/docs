---
title: "Get user live video data"
description: "Get user live video data"
openapi: "openapi/tikhub.json GET /tikhub/douyin/web/fetch_user_live_videos"
---

Get user live video data

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/web/fetch_user_live_videos?webcast_id=376034101029" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
