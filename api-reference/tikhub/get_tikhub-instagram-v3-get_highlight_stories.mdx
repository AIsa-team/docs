---
title: "Get highlight stories"
description: "Get highlight stories"
openapi: "openapi/tikhub-en.json GET /tikhub/instagram/v3/get_highlight_stories"
---

Get highlight stories

**Pricing** — base $0.0116 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/instagram/v3/get_highlight_stories?highlight_id=highlight:18064916456320419" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
