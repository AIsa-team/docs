---
title: "Get creators with similar content"
description: "Get creators with similar content"
openapi: "openapi/tikhub-en.json GET /tikhub/bilibili/huahuo/upper_similar"
---

Get creators with similar content

**Pricing** — base $0.0087 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/huahuo/upper_similar?upper_mid=1" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
