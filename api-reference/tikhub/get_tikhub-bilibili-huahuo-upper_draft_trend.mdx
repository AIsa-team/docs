---
title: "Get creator video metric trends"
description: "Get creator video metric trends"
openapi: "openapi/tikhub-en.json GET /tikhub/bilibili/huahuo/upper_draft_trend"
---

Get creator video metric trends

**Pricing** — base $0.0087 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/huahuo/upper_draft_trend?upper_mid=1" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
