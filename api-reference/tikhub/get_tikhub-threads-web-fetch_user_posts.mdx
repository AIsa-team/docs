---
title: "Get user posts (no pagination)"
description: "Get user posts (no pagination)"
openapi: "openapi/tikhub-en.json GET /tikhub/threads/web/fetch_user_posts"
---

Get user posts (no pagination)

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/threads/web/fetch_user_posts?user_id=63625256886" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
