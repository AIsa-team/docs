---
title: "Get recommended ads"
description: "获取推荐广告/Get recommended ads"
openapi: "openapi/tikhub.json POST /tikhub/tiktok/ads/get_recommended_ads"
---

获取推荐广告/Get recommended ads

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/tiktok/ads/get_recommended_ads" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
