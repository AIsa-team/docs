---
title: "Generate XGnarly"
description: "生成 XGnarly /Generate XGnarly"
openapi: "openapi/tikhub.json POST /tikhub/tiktok/web/generate_xgnarly"
---

生成 XGnarly /Generate XGnarly

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/tiktok/web/generate_xgnarly" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
