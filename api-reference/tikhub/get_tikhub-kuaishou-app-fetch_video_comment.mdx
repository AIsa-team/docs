---
title: "Get single video comment data"
description: "获取单个作品评论数据/Get single video comment data"
openapi: "openapi/tikhub.json GET /tikhub/kuaishou/app/fetch_video_comment"
---

获取单个作品评论数据/Get single video comment data

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/kuaishou/app/fetch_video_comment?photo_id=3x7gxp2zhgjv832" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
