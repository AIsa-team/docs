---
title: "Live room start status check"
description: "直播间开播状态检测/Live room start status check"
openapi: "openapi/tikhub.json GET /tikhub/tiktok/web/fetch_check_live_alive"
---

直播间开播状态检测/Live room start status check

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/web/fetch_check_live_alive?room_id=7381444193462078214" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
