---
title: "Fetch activity calendar detail"
description: "Fetch activity calendar detail"
openapi: "openapi/tikhub.json GET /tikhub/douyin/billboard/fetch_hot_calendar_detail"
---

Fetch activity calendar detail

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/billboard/fetch_hot_calendar_detail?calendar_id=1720" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
