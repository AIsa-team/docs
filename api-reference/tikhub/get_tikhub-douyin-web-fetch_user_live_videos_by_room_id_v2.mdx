---
title: "Gets the live stream data of the specified user by room_id V2"
description: "Gets the live stream data of the specified user by room_id V2"
openapi: "openapi/tikhub.json GET /tikhub/douyin/web/fetch_user_live_videos_by_room_id_v2"
---

Gets the live stream data of the specified user by room_id V2

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/web/fetch_user_live_videos_by_room_id_v2?room_id=7462723839303093032" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
