---
title: "Batch check if live rooms are online"
description: "批量检测直播间是否在线/Batch check if live rooms are online"
openapi: "openapi/tikhub.json POST /tikhub/tiktok/app/v3/check_live_room_online_batch"
---

批量检测直播间是否在线/Batch check if live rooms are online

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/tiktok/app/v3/check_live_room_online_batch" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
