---
title: "Get commercial tags"
description: "Get commercial tags"
openapi: "openapi/tikhub-en.json GET /tikhub/bilibili/huahuo/tag_commercial"
---

Get commercial tags

**Pricing** — base $0.0087 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/huahuo/tag_commercial" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
