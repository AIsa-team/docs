---
title: "Get video featured feed"
description: "Get video featured feed"
openapi: "openapi/tikhub.json GET /tikhub/weibo/app/fetch_video_featured_feed"
---

Get video featured feed

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/weibo/app/fetch_video_featured_feed" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
