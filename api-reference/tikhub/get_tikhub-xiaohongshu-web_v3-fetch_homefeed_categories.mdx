---
title: "Fetch homepage categories"
description: "获取首页分类列表/Fetch homepage categories"
openapi: "openapi/tikhub.json GET /tikhub/xiaohongshu/web_v3/fetch_homefeed_categories"
---

获取首页分类列表/Fetch homepage categories

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/web_v3/fetch_homefeed_categories" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
