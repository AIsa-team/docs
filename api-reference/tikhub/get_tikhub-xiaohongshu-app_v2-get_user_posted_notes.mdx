---
title: "Get user posted notes"
description: "获取用户笔记列表/Get user posted notes"
openapi: "openapi/tikhub.json GET /tikhub/xiaohongshu/app_v2/get_user_posted_notes"
---

获取用户笔记列表/Get user posted notes

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/app_v2/get_user_posted_notes" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
