---
title: "Get general search data"
description: "Get general search data"
openapi: "openapi/tikhub-en.json GET /tikhub/bilibili/web/fetch_general_search"
---

Get general search data

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/web/fetch_general_search?keyword=Naruto&order=totalrank&page=1&page_size=42" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
