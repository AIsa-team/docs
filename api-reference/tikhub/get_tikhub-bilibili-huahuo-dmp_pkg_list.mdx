---
title: "Get audience packages"
description: "人群包列表/Get audience packages"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/huahuo/dmp_pkg_list"
---

人群包列表/Get audience packages

**Pricing** — base $0.0087 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/huahuo/dmp_pkg_list" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
