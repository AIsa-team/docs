---
title: "Douyin APP register device"
description: "Douyin APP register device"
openapi: "openapi/tikhub.json GET /tikhub/douyin/app/v3/register_device"
---

Douyin APP register device

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/app/v3/register_device" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
