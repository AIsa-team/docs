---
title: "Get hot search brief"
description: "Get hot search brief"
openapi: "openapi/tikhub.json GET /tikhub/weibo/app/fetch_hot_search_brief"
---

Get hot search brief

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/weibo/app/fetch_hot_search_brief" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
