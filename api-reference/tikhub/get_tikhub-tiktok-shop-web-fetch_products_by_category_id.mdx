---
title: "Get products by category ID"
description: "根据分类ID获取商品列表/Get products by category ID"
openapi: "openapi/tikhub.json GET /tikhub/tiktok/shop/web/fetch_products_by_category_id"
---

根据分类ID获取商品列表/Get products by category ID

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/shop/web/fetch_products_by_category_id?category_id=963976" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
