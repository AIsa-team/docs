---
title: "Fetch search suggestions"
description: "Fetch search suggestions"
openapi: "openapi/tikhub-en.json GET /tikhub/xiaohongshu/web_v3/fetch_search_suggest"
---

Fetch search suggestions

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/web_v3/fetch_search_suggest" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
