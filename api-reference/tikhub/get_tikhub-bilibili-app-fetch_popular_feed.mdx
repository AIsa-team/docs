---
title: "Get popular feed"
description: "获取热门推荐/Get popular feed"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/app/fetch_popular_feed"
---

获取热门推荐/Get popular feed

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/app/fetch_popular_feed" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
