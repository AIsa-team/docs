---
title: "Get video note detail"
description: "获取视频笔记详情/Get video note detail"
openapi: "openapi/tikhub.json GET /tikhub/xiaohongshu/app_v2/get_video_note_detail"
---

获取视频笔记详情/Get video note detail

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/app_v2/get_video_note_detail" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
