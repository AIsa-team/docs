---
title: "Batch get the highest quality play URL of videos"
description: "批量获取视频的最高画质播放链接/Batch get the highest quality play URL of videos"
openapi: "openapi/tikhub.json POST /tikhub/douyin/app/v3/fetch_multi_video_high_quality_play_url"
---

批量获取视频的最高画质播放链接/Batch get the highest quality play URL of videos

**Pricing** — base $0.3625 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/douyin/app/v3/fetch_multi_video_high_quality_play_url" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
