---
title: "PGY keyword overview"
description: "蒲公英关键词概览（搜索指数+博主画像）/PGY keyword overview"
openapi: "openapi/tikhub.json POST /tikhub/xiaohongshu/pgy/get_keyword_stat"
---

蒲公英关键词概览（搜索指数+博主画像）/PGY keyword overview

**Pricing** — base $0.029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/pgy/get_keyword_stat" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
