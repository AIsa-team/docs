---
title: "Get user live info"
description: "Get user live info"
openapi: "openapi/tikhub-en.json GET /tikhub/kuaishou/app/fetch_user_live_info"
---

Get user live info

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/kuaishou/app/fetch_user_live_info?user_id=1377082950" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
