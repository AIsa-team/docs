---
title: "Get information of specified video"
description: "Get information of specified video"
openapi: "openapi/tikhub-en.json GET /tikhub/toutiao/web/get_video_info"
---

Get information of specified video

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/toutiao/web/get_video_info?aweme_id=7431543350882206242" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
