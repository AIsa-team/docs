---
title: "Get general search list"
description: "获取综合搜索列表/Get general search list"
openapi: "openapi/tikhub.json GET /tikhub/tiktok/web/fetch_general_search"
---

获取综合搜索列表/Get general search list

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/web/fetch_general_search?keyword=TikTok" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
