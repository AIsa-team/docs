---
title: "Get live room gift list"
description: "获取直播间礼物列表/Get live room gift list"
openapi: "openapi/tikhub.json GET /tikhub/tiktok/web/fetch_live_gift_list"
---

获取直播间礼物列表/Get live room gift list

**Pricing** — base $0.03625 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/web/fetch_live_gift_list" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
