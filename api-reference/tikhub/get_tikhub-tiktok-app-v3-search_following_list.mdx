---
title: "Search following list"
description: "Search following list"
openapi: "openapi/tikhub-en.json GET /tikhub/tiktok/app/v3/search_following_list"
---

Search following list

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/app/v3/search_following_list?user_id=7540849481009988663&keyword=a" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
