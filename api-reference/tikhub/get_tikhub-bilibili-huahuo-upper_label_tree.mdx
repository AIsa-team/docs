---
title: "Get creator filter panel configuration"
description: "搜UP主筛选面板配置/Get creator filter panel configuration"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/huahuo/upper_label_tree"
---

搜UP主筛选面板配置/Get creator filter panel configuration

**Pricing** — base $0.0087 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/huahuo/upper_label_tree" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
