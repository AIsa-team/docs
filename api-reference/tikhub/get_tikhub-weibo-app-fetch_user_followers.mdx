---
title: "Get user followers"
description: "获取用户粉丝列表/Get user followers"
openapi: "openapi/tikhub.json GET /tikhub/weibo/app/fetch_user_followers"
---

获取用户粉丝列表/Get user followers

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/weibo/app/fetch_user_followers?uid=1856404484" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
