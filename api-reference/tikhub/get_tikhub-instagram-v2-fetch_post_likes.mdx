---
title: "Get post likes"
description: "Get post likes"
openapi: "openapi/tikhub-en.json GET /tikhub/instagram/v2/fetch_post_likes"
---

Get post likes

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/instagram/v2/fetch_post_likes?code_or_url=DRhvwVLAHAG" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
