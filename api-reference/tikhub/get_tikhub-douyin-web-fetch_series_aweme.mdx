---
title: "Series Video"
description: "Series Video"
openapi: "openapi/tikhub-en.json GET /tikhub/douyin/web/fetch_series_aweme"
---

Series Video

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/web/fetch_series_aweme?offset=0&count=16&content_type=0" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
