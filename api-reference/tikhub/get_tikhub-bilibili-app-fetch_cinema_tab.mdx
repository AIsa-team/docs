---
title: "Get cinema tab"
description: "获取影视推荐/Get cinema tab"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/app/fetch_cinema_tab"
---

获取影视推荐/Get cinema tab

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/app/fetch_cinema_tab" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
