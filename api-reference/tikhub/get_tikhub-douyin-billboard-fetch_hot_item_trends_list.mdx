---
title: "Fetch post data trend"
description: "获取作品数据趋势/Fetch post data trend"
openapi: "openapi/tikhub.json GET /tikhub/douyin/billboard/fetch_hot_item_trends_list"
---

获取作品数据趋势/Fetch post data trend

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/billboard/fetch_hot_item_trends_list" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
