---
title: "Get creator marketplace filter dictionaries"
description: "广场筛选字典/Get creator marketplace filter dictionaries"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/huahuo/square_static_info"
---

广场筛选字典/Get creator marketplace filter dictionaries

**Pricing** — base $0.0087 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/huahuo/square_static_info" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
