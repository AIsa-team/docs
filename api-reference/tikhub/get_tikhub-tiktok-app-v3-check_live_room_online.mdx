---
title: "Check if live room is online"
description: "Check if live room is online"
openapi: "openapi/tikhub-en.json GET /tikhub/tiktok/app/v3/check_live_room_online"
---

Check if live room is online

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/app/v3/check_live_room_online?room_id=7358603858249009962" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
