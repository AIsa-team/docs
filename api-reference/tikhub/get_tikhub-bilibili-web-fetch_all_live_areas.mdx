---
title: "Get a list of all live areas"
description: "获取所有直播分区列表/Get a list of all live areas"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/web/fetch_all_live_areas"
---

获取所有直播分区列表/Get a list of all live areas

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/web/fetch_all_live_areas" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
