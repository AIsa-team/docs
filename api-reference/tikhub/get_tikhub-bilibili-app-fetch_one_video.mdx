---
title: "Get single video data"
description: "获取单个视频详情信息/Get single video data"
openapi: "openapi/tikhub.json GET /tikhub/bilibili/app/fetch_one_video"
---

获取单个视频详情信息/Get single video data

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/bilibili/app/fetch_one_video" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
