---
title: "Get WeChat MP Article Ads"
description: "Get WeChat MP Article Ads"
openapi: "openapi/tikhub-en.json POST /tikhub/wechat_mp/v2/fetch_article_ad"
---

Get WeChat MP Article Ads

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/wechat_mp/v2/fetch_article_ad" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
