---
title: "Convert shortcode to media ID"
description: "Convert shortcode to media ID"
openapi: "openapi/tikhub-en.json GET /tikhub/instagram/v3/shortcode_to_media_id"
---

Convert shortcode to media ID

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/instagram/v3/shortcode_to_media_id?shortcode=CrgVBtHrFHm" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
