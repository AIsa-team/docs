---
title: "Get Search Field Options"
description: "Get Search Field Options"
openapi: "openapi/tikhub-en.json GET /tikhub/douyin/xingtu_v2/get_search_field_options"
---

Get Search Field Options

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/xingtu_v2/get_search_field_options" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
