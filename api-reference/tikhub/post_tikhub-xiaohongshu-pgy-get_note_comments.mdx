---
title: "Get PGY note featured comments"
description: "获取蒲公英笔记精选评论/Get PGY note featured comments"
openapi: "openapi/tikhub.json POST /tikhub/xiaohongshu/pgy/get_note_comments"
---

获取蒲公英笔记精选评论/Get PGY note featured comments

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/pgy/get_note_comments" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
