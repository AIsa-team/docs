---
title: "TikTok APP encryption algorithm"
description: "TikTok APP encryption algorithm"
openapi: "openapi/tikhub-en.json POST /tikhub/tiktok/app/v3/TTencrypt_algorithm"
---

TikTok APP encryption algorithm

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/tiktok/app/v3/TTencrypt_algorithm" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
