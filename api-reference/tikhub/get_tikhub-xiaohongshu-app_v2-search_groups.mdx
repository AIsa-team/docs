---
title: "Search groups"
description: "Search groups"
openapi: "openapi/tikhub-en.json GET /tikhub/xiaohongshu/app_v2/search_groups"
---

Search groups

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/app_v2/search_groups?keyword=Shanghai" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
