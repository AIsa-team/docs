---
title: "Get Item Play Count"
description: "获取单个视频播放量(含详情)/Get Item Play Count"
openapi: "openapi/tikhub.json GET /tikhub/douyin/xingtu_v2/get_item_play_count"
---

获取单个视频播放量(含详情)/Get Item Play Count

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/xingtu_v2/get_item_play_count?item_id=7630036460159111515" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
