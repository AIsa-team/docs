---
title: "Get WeChat Channels Live History"
description: "Get WeChat Channels Live History"
openapi: "openapi/tikhub-en.json POST /tikhub/wechat_channels/v2/fetch_live_history"
---

Get WeChat Channels Live History

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/wechat_channels/v2/fetch_live_history" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
