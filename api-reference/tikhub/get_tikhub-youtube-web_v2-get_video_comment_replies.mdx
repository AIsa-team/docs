---
title: "Get video sub comments"
description: "Get video sub comments"
openapi: "openapi/tikhub-en.json GET /tikhub/youtube/web_v2/get_video_comment_replies"
---

Get video sub comments

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/youtube/web_v2/get_video_comment_replies?continuation_token=VALUE" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
