---
title: "Detect fake views in video"
description: "检测视频虚假流量分析/Detect fake views in video"
openapi: "openapi/tikhub.json GET /tikhub/tiktok/analytics/detect_fake_views"
---

检测视频虚假流量分析/Detect fake views in video

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **以实际调用为准 / refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/tiktok/analytics/detect_fake_views?item_id=7460937381265411370" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
