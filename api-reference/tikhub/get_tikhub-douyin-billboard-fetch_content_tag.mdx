---
title: "Fetch vertical content tags"
description: "获取垂类内容标签/Fetch vertical content tags"
openapi: "openapi/tikhub.json GET /tikhub/douyin/billboard/fetch_content_tag"
---

获取垂类内容标签/Fetch vertical content tags

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/billboard/fetch_content_tag" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
