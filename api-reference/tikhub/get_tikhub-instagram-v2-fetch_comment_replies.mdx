---
title: "Get comment replies"
description: "Get comment replies"
openapi: "openapi/tikhub-en.json GET /tikhub/instagram/v2/fetch_comment_replies"
---

Get comment replies

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/instagram/v2/fetch_comment_replies?code_or_url=DRhvwVLAHAG&comment_id=18067775592012345" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
