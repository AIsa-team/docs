---
title: "Fetch city hot list"
description: "Fetch city hot list"
openapi: "openapi/tikhub.json GET /tikhub/douyin/billboard/fetch_hot_city_list"
---

Fetch city hot list

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/billboard/fetch_hot_city_list?page=1&page_size=10&order=VALUE" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
