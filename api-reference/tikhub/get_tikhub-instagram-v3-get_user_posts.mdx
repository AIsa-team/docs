---
title: "Get user posts"
description: "Get user posts"
openapi: "openapi/tikhub-en.json GET /tikhub/instagram/v3/get_user_posts"
---

Get user posts

**Pricing** — base $0.0116 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/instagram/v3/get_user_posts?username=99brasil" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
