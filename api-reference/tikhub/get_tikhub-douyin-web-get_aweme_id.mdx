---
title: "Extract single video id"
description: "Extract single video id"
openapi: "openapi/tikhub-en.json GET /tikhub/douyin/web/get_aweme_id"
---

Extract single video id

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/web/get_aweme_id?url=https://www.douyin.com/video/7298145681699622182" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
