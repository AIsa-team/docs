---
title: "Multi Get Item Stats"
description: "Multi Get Item Stats"
openapi: "openapi/tikhub.json POST /tikhub/douyin/xingtu_v2/multi_get_item_stats"
---

Multi Get Item Stats

**Pricing** — base $0.058 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/douyin/xingtu_v2/multi_get_item_stats" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # see request schema below
```
