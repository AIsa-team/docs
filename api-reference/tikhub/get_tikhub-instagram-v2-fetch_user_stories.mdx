---
title: "Get user stories"
description: "Get user stories"
openapi: "openapi/tikhub-en.json GET /tikhub/instagram/v2/fetch_user_stories"
---

Get user stories

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/instagram/v2/fetch_user_stories" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
