---
title: "Get all valid dates"
description: "Get all valid dates"
openapi: "openapi/tikhub-en.json GET /tikhub/douyin/index/fetch_all_valid_date"
---

Get all valid dates

**Pricing** — base $0.00145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/index/fetch_all_valid_date" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
