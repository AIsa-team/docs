---
title: "Search music"
description: "Search music"
openapi: "openapi/tikhub-en.json GET /tikhub/kuaishou/app/search_music"
---

Search music

**Pricing** — base $0.0145 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/kuaishou/app/search_music?keyword=Jay Chou" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
