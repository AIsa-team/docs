---
title: "Get PGY blogger filter options"
description: "Get PGY blogger filter options"
openapi: "openapi/tikhub.json POST /tikhub/xiaohongshu/pgy/get_blogger_filter_options"
---

Get PGY blogger filter options

**Pricing** — base $0.029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

## Example

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/pgy/get_blogger_filter_options" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'
```
