---
title: "Get post comments (no pagination)"
description: "Get post comments (no pagination)"
openapi: "openapi/tikhub-en.json GET /tikhub/threads/web/fetch_post_comments"
---

Get post comments (no pagination)

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/threads/web/fetch_post_comments?post_id=3390920896561588969" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
