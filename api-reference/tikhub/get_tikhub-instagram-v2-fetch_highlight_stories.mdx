---
title: "Get highlight stories"
description: "Get highlight stories"
openapi: "openapi/tikhub-en.json GET /tikhub/instagram/v2/fetch_highlight_stories"
---

Get highlight stories

**Pricing** — base $0.0029 per successful call (provider cost × 1.45; final charge scales by your plan multiplier). Charged only when the upstream response body `code` is `200`.

<Note>
Response structure is not yet fully documented — **refer to the actual API response**.
</Note>

## Example

```bash
curl "https://api.aisa.one/apis/v1/tikhub/instagram/v2/fetch_highlight_stories?highlight_id=17895069621772257" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
