---
title: "Backlinks"
description: "Individual backlink records pointing at a target (source, target, anchor)."
openapi: "openapi/semrush.json GET /semrush/backlinks"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Individual backlink records pointing at a target (source, target, anchor).

**Billing is per returned data row**, \$0.36 per row (up to 20 rows). A broad seed returns more rows, so the input size decides the cost — start narrow.

⚠️ The response is **semicolon-delimited text**, not JSON — parse it as CSV-style rows.

Related: [`get_semrush-referring-domains`](/api-reference/semrush/get_semrush-referring-domains), [`get_semrush-backlink-anchors`](/api-reference/semrush/get_semrush-backlink-anchors).
