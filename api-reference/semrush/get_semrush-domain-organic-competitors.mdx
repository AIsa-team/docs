---
title: "Organic Competitors"
description: "Domains competing with a target in Google organic search."
openapi: "openapi/semrush.json GET /semrush/domain-organic-competitors"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Domains competing with a target in Google organic search.

**Billing is per returned data row**, $0.36 per row (up to 20 rows). A broad seed returns more rows, so the input size decides the cost — start narrow.

⚠️ The response is **semicolon-delimited text**, not JSON — parse it as CSV-style rows.

Related: [`get_semrush-domain-vs-domain`](/api-reference/semrush/get_semrush-domain-vs-domain).
