---
title: "Domain Organic Keywords"
description: "Keywords a domain ranks for in Google organic (position, volume, CPC, URL)."
openapi: "openapi/semrush.json GET /semrush/domain-organic-keywords"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Keywords a domain ranks for in Google organic (position, volume, CPC, URL).

**Billing is per returned data row**, \$0.09 per row (up to 20 rows). A broad seed returns more rows, so the input size decides the cost — start narrow.

⚠️ The response is **semicolon-delimited text**, not JSON — parse it as CSV-style rows.

Related: [`get_semrush-domain-paid-keywords`](/api-reference/semrush/get_semrush-domain-paid-keywords), [`get_semrush-url-organic-keywords`](/api-reference/semrush/get_semrush-url-organic-keywords).
