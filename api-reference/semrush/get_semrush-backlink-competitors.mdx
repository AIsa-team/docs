---
title: "Backlink Competitors"
description: "Domains with a backlink profile similar to the target."
openapi: "openapi/semrush.json GET /semrush/backlink-competitors"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Domains with a backlink profile similar to the target.

**Billing is per returned data row**, \$0.36 per row (up to 20 rows). A broad seed returns more rows, so the input size decides the cost — start narrow.

⚠️ The response is **semicolon-delimited text**, not JSON — parse it as CSV-style rows.

Related: [`get_semrush-referring-domains`](/api-reference/semrush/get_semrush-referring-domains).
