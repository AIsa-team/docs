---
title: "Domain vs Domain"
description: "Cross-domain keyword position comparison across up to 5 domains."
openapi: "openapi/semrush.json GET /semrush/domain-vs-domain"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Cross-domain keyword position comparison across up to 5 domains.

**Billing is per returned data row**, \$0.72 per row (up to 20 rows). A broad seed returns more rows, so the input size decides the cost — start narrow.

⚠️ The response is **semicolon-delimited text**, not JSON — parse it as CSV-style rows.

Related: [`get_semrush-domain-organic-competitors`](/api-reference/semrush/get_semrush-domain-organic-competitors).
