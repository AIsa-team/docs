---
title: "Broad Match Keywords"
description: "Broad-match and alternate keyword variants for a seed phrase, with volume and CPC."
openapi: "openapi/semrush.json GET /semrush/broad-match-keywords"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Broad-match and alternate keyword variants for a seed phrase, with volume and CPC.

**Billing is per returned data row**, $0.18 per row (up to 20 rows). A broad seed returns more rows, so the input size decides the cost — start narrow.

⚠️ The response is **semicolon-delimited text**, not JSON — parse it as CSV-style rows.

Related: [`get_semrush-question-keywords`](/api-reference/semrush/get_semrush-question-keywords), [`get_semrush-keyword-overview`](/api-reference/semrush/get_semrush-keyword-overview).
