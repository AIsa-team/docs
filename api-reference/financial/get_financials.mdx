---
title: "All Financial Statements"
description: "All three financial statements for one company in a single call."
openapi: "openapi/openapi-financial.json GET /financials"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

All three statements for one company in a single call. Returns a `financials` object holding `income_statements`, `balance_sheets` and `cash_flow_statements`, each the same shape the dedicated tools return. `period` is required (annual, quarterly or ttm); identify the company by `ticker` or `cik` and cap with `limit`. Use it when you need the full picture and would otherwise make three calls. When you only need one statement, [`get_financial_financials_income_statements`](/api-reference/financial/get_financials-income-statements), [`get_financial_financials_balance_sheets`](/api-reference/financial/get_financials-balance-sheets) or [`get_financial_financials_cash_flow_statements`](/api-reference/financial/get_financials-cash-flow-statements) returns far less data; when you need a handful of named fields across several companies, [`post_financial_financials_search_line_items`](/api-reference/financial/post_financials-search-line-items) is narrower still.
