---
title: "Get User Last Tweets"
description: "Get a user's most recent tweets, accepting either a @handle or a numeric user ID."
openapi: "openapi/twitter-user-batch_01.json GET /twitter/user/last_tweets"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Get a user's most recent tweets, accepting either `userName` (@handle) or `userId` — useful when you have not resolved the handle to an ID yet. Optionally include replies. Cursor-paginated.

Returns tweets under `data` with `has_next_page` and `next_cursor`. Use [`get_twitter_user_tweet_timeline`](/api-reference/twitter/get_twitter-user-tweet-timeline) instead when you already have the numeric ID and want the full profile-order timeline with parent-tweet expansion.
