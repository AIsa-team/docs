---
title: "Get User Followings"
description: "List the accounts a given X user follows, identified by @handle."
openapi: "openapi/twitter-user-batch_02.json GET /twitter/user/followings"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

List the accounts a given X user follows, identified by @handle.

Returns 200 entries per page with `has_next_page` and `next_cursor`. Use this to infer a user's interests, information sources, or professional network — who someone follows is usually a stronger signal of intent than who follows them.

For the opposite direction use [`get_twitter_user_followers`](/api-reference/twitter/get_twitter-user-followers). To test a single specific pair without paging through thousands of records, use [`get_twitter_user_check_follow_relationship`](/api-reference/twitter/get_twitter-user-check-follow-relationship).
