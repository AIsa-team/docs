---
title: "Get Tweet Thread Context"
description: "Reconstruct the conversation around a tweet."
openapi: "openapi/twitter-tweet-batch_02.json GET /twitter/tweet/thread_context"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Reconstruct the conversation around a tweet. Accepts either a reply or an original tweet and returns the surrounding thread, cursor-paginated. Use this when a tweet lacks context on its own and you need the parent chain to interpret it correctly.

Returns tweets under `tweets` with `has_next_page` and `next_cursor`.

For only the replies below a post use [`get_twitter_tweet_replies_v2`](/api-reference/twitter/get_twitter-tweet-replies-v2).
