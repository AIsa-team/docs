---
title: "Create Deal"
description: "Create a deal."
openapi: "openapi/apollo.json POST /apollo/opportunities"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Create a deal. Stage ids come from [`get_apollo_opportunity_stages`](/api-reference/apollo/get_apollo-opportunity-stages), and the account it belongs to comes from [`post_apollo_accounts_search`](/api-reference/apollo/post_apollo-accounts-search). Writes land in the AIsa workspace, which every caller shares: the record becomes visible and editable by others, and there is no per-caller isolation. Deals feed the workspace's forecast, so a test record distorts numbers other people read.
