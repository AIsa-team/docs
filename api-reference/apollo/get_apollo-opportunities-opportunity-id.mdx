---
title: "View Deal"
description: "One deal by its Apollo id, with the full field set."
openapi: "openapi/apollo.json GET /apollo/opportunities/{opportunity_id}"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

One deal by its Apollo id, with the full field set. Find the id with [`get_apollo_opportunities_search`](/api-reference/apollo/get_apollo-opportunities-search).
