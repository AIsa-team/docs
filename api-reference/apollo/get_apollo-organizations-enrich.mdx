---
title: "Organization Enrichment"
description: "Enrich one company by domain."
openapi: "openapi/apollo.json GET /apollo/organizations/enrich"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Enrich one company by domain. `domain` is the only parameter and it must be the bare domain (apple.com), not a full URL. Returns an `organization` object with `id`, `name`, `website_url`, `linkedin_url`, `twitter_url`, `facebook_url`, `angellist_url`, `phone`, `founded_year`, `alexa_ranking`, `publicly_traded_symbol`, `publicly_traded_exchange` and `languages`. Use it as the entry point when all you have is a domain. For several domains at once use [`post_apollo_organizations_bulk_enrich`](/api-reference/apollo/post_apollo-organizations-bulk-enrich); for the full record including funding and technology detail use [`get_apollo_organizations_id`](/api-reference/apollo/get_apollo-organizations-id), which needs the Apollo organization id this call returns.
