---
title: "Create an Account"
description: "Create an account — a company saved into this workspace."
openapi: "openapi/apollo.json POST /apollo/accounts"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Create an account — a company saved into this workspace. Duplicate domains are rejected, so search with [`post_apollo_accounts_search`](/api-reference/apollo/post_apollo-accounts-search) before creating. Requires a master API key. Writes land in the AIsa workspace, which every caller shares: the record becomes visible and editable by others, and there is no per-caller isolation. To enrich a company without saving it, use [`get_apollo_organizations_enrich`](/api-reference/apollo/get_apollo-organizations-enrich).
