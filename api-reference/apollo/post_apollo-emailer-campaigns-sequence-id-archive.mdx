---
title: "Archive a Sequence"
description: "Archive a sequence, removing it from the active list while keeping its history."
openapi: "openapi/apollo.json POST /apollo/emailer_campaigns/{sequence_id}/archive"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Archive a sequence, removing it from the active list while keeping its history. Archiving does not stop an active sequence on its own — deactivate it with [`post_apollo_emailer_campaigns_sequence_id_abort`](/api-reference/apollo/post_apollo-emailer-campaigns-sequence-id-abort) first if it is still sending. Writes land in the AIsa workspace, which every caller shares: the record becomes visible and editable by others, and there is no per-caller isolation.
