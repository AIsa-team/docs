---
title: "Search for Tasks"
description: "Search tasks in this workspace."
openapi: "openapi/apollo.json POST /apollo/tasks/search"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Search tasks in this workspace. Returns `tasks` with `pagination`, `breadcrumbs`, `faceting` and `pipeline_total`. ⚠️ Shared workspace: results include tasks other AIsa callers created.
