---
title: "View a Contact"
description: "One saved contact by its Apollo id, with the full field set including custom fields, owner and stage."
openapi: "openapi/apollo.json GET /apollo/contacts/{contact_id}"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

One saved contact by its Apollo id, with the full field set including custom fields, owner and stage. Find the id with [`post_apollo_contacts_search`](/api-reference/apollo/post_apollo-contacts-search). Reads the shared workspace, not Apollo's global database.
