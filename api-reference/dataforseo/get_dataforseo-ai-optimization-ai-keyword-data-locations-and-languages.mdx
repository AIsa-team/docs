---
title: "List of Locations and Languages for AI Keyword Data API"
description: "The locations and languages the AI keyword endpoint accepts, as location_code, location_name and available_languages nested per location."
openapi: "openapi/dataforseo.json GET /dataforseo/ai_optimization/ai_keyword_data/locations_and_languages"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

The locations and languages the AI keyword endpoint accepts, as `location_code`, `location_name` and `available_languages` nested per location. Measured at 13.1 KB. Free: upstream cost is 0. Reference data - fetch once and reuse.

## Example

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/ai_optimization/ai_keyword_data/locations_and_languages" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
