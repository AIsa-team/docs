---
title: "Tavily Crawl"
description: "Walk a site from a root url and return the content of the pages it finds."
openapi: "openapi/tavily.json POST /tavily/crawl"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Walk a site from a root `url` and return the content of the pages it finds. Steer it with natural-language `instructions` plus regex path and domain filters, and bound it with `max_depth`, `max_breadth` and `limit`. Returns `base_url` and `results[]` with `url` and `raw_content`. Measured at about 4.5 seconds for a 3-page limit; cost and time grow with the bounds you set, so set them. Use it for broad coverage of one site — documentation, a catalogue, a competitor's blog. It answers synchronously, which `post_firecrawl_crawl` does not: that one runs as a background job and suits crawls too large to wait on. For a handful of known pages [`post_tavily_extract`](/api-reference/search/post_tavily-extract) is far cheaper; to size a site before paying to crawl it, run [`post_tavily_map`](/api-reference/search/post_tavily-map) first.
