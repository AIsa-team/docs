---
title: "Firecrawl Crawl"
description: "Crawl a whole site rooted at url and return the content of every page it keeps."
openapi: "openapi/firecrawl.json POST /firecrawl/crawl"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Crawl a whole site rooted at `url` and return the content of every page it keeps. `url`, `limit` and an `Idempotency-Key` are required; steer it with `includePaths`, `excludePaths`, `maxDiscoveryDepth`, `crawlEntireDomain`, `allowSubdomains`, `delay` and `maxConcurrency`. Asynchronous. Submitting returns HTTP 202 and a job envelope — `id`, `object`, `endpoint`, `status`, `createdAt`, `completedAt`, `pricing`, `output`, `error` — with `output` still null. Poll [`get_firecrawl_crawl_job`](/api-reference/search/get_firecrawl-crawl-job) until `status` is `completed`, `failed` or `cancelled`; `output` is then an array of pages, each with `markdown` and `metadata`. A 3-page crawl measured 43 KB and finished in under a minute, and `pricing.billingMode` is `metered_result`, so **cost scales with what it finds** — set `limit`. Send a fresh `Idempotency-Key` per distinct crawl; reusing one returns the earlier job instead of starting a new one. For a handful of known URLs [`post_firecrawl_batch_scrape`](/api-reference/search/post_firecrawl-batch-scrape) is cheaper, and for structure alone [`post_firecrawl_map`](/api-reference/search/post_firecrawl-map) costs far less.
