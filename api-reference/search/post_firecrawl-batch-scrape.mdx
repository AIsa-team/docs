---
title: "Firecrawl Batch Scrape"
description: "Scrape many URLs as one background job."
openapi: "openapi/firecrawl.json POST /firecrawl/batch-scrape"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Scrape many URLs as one background job. `urls` and an `Idempotency-Key` are required; `maxConcurrency`, `onlyMainContent`, `includeTags`, `excludeTags`, `maxAge`, `minAge` and `timeout` tune it. Asynchronous. Submitting returns HTTP 202 and a job envelope — `id`, `object`, `endpoint`, `status`, `createdAt`, `completedAt`, `pricing`, `output`, `error` — with `output` still null. Poll [`get_firecrawl_batch_scrape_job`](/api-reference/search/get_firecrawl-batch-scrape-job) until terminal; `output` is then an array of documents with `markdown` and `metadata`. Use it when you have a list of URLs and do not need them immediately. When you do need them immediately, [`post_tavily_extract`](/api-reference/search/post_tavily-extract) returns a small batch synchronously in about a second; for a single page [`post_firecrawl_scrape`](/api-reference/search/post_firecrawl-scrape). Send a fresh `Idempotency-Key` per distinct batch.
