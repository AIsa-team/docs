---
title: "Install AIsa CIO (OpenClaw)"
description: "Install AIsa CIO as an OpenClaw agent for live portfolio valuation, deep stock research, technical scans, and policy-driven allocation."
---

# Install AIsa CIO (OpenClaw agent)

AI Chief Investment Officer — live multi-market portfolio valuation, deep stock research, technical scans, policy-driven allocation decisions. Responds in English.

## Prerequisites
- [OpenClaw](https://openclaw.ai) installed (`npm install -g openclaw`), gateway configured (`openclaw onboard`)
- `python3`, `curl`, `tar` on PATH

## Environment variables
| var | need | description |
|---|---|---|
| `AISA_API_KEY` | required | AISA multi-model gateway — default LLM + the agent-skills data skills (marketpulse / multi-source-search / prediction-market-data / aisa-twitter-api / last30days) |
| `SEC_IDENTITY` | optional | Your own name + email, e.g. "Jane Doe jane@example.com". The SEC requires every automated request for filings to declare a real contact, and caps each declared identity at 10 requests/second. Not an API key — there is nothing to sign up for, and the value is sent verbatim without validation. Used only by the optional sec-filings skill. In a hermes/openclaw .env it MUST be quoted, since it contains spaces and the file is shell-sourced: SEC_IDENTITY="Jane Doe jane@example.com" — degrade: sec-filings skill (deep SEC filing research) is unavailable; marketpulse still covers prices and quick financials |
| `AISA_MODEL` | optional | Reasoning model id for the last30days 30-day scan, e.g. deepseek-v4-pro. Add it to ~/.aisa/credentials as AISA_MODEL=<id>. — degrade: last30days refuses to run without a model pin; every other skill is unaffected |

Skills resolve each variable as: environment variable → `~/.aisa/credentials`
(one `KEY=VALUE` per line, `chmod 600`). The simplest path: after install, just
run a command that needs the key — the agent walks you through setup in the
conversation and writes the credentials file for you (takes effect immediately,
no restart).
- No `AISA_API_KEY` yet? Get one at https://console.aisa.one/get-started (takes about a minute).

## Install

Do **not** re-implement the steps by hand. Download and run the installer (it
fetches the artifact, verifies `sha256`, seeds the agent workspace at
`~/.openclaw/workspace-aisa-cio`, registers the isolated agent, writes
`~/.aisa/credentials`, seeds the user data directory, and registers cron jobs
when the gateway is reachable). Export required vars first if the session is
non-interactive.

```bash
curl -fsSL "https://github.com/AIsa-team/agent-index/releases/download/aisa-cio-v1.7.6/install-openclaw.sh" | bash
```

Artifact (fetched by the installer): `https://github.com/AIsa-team/agent-index/releases/download/aisa-cio-v1.7.6/aisa-cio-openclaw-v1.7.6.tar.gz`  
Checksum: `2be507f58d15f4180cf9ccee6c343524eae1f69c528b82136ac70e766e6e4e64`

Note: pre-approved commands are expressed as AGENTS.md guidance; OpenClaw's
exec approvals may still prompt on first use depending on your tool policy.

Note: the installer also sets `agents.defaults.skipBootstrap=true` globally
(OpenClaw has no per-agent scope), so newly created agents will skip the
bootstrap ritual too; unset it with `openclaw config unset agents.defaults.skipBootstrap`
if you don't want that.

## Verify

```bash
openclaw agent --agent aisa-cio -m "hello, who are you?"
```
