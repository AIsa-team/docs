# ------------------------------------------------------------------
# Sync the consolidated OpenAPI spec and notify downstream consumers:
#
#   1. This repo's root (openapi.yaml) — public download URL
#      https://raw.githubusercontent.com/AIsa-team/docs/main/openapi.yaml
#   2. The aisa-tool-router repo — receives a repository_dispatch after
#      the generated docs commit is available on main.
#
# AIsa-team/new-style-landing-page 已归档，因此不再向该仓库同步
# client/public/openapi.yaml；继续 checkout 或 push 归档仓库会导致此 CI 失败。
#
# Triggers:
#   - Push to main that touches any file in openapi/
#   - Manual dispatch (workflow_dispatch)
#
# The auto-commit back to this repo writes openapi.yaml at the root,
# which is NOT under the trigger path filter (`openapi/**`), so the
# workflow does not re-trigger itself.
#
# Required secrets:
#   TOOL_ROUTER_DISPATCH_APP_PRIVATE_KEY — PEM private key for the installed
#   AIsa-team/aisa-release-bot GitHub App
#   SLACK_WEBHOOK_URL — Slack incoming webhook for notifications
# Required variables:
#   TOOL_ROUTER_DISPATCH_APP_ID — App ID for AIsa-team/aisa-release-bot
# ------------------------------------------------------------------

name: Sync OpenAPI Spec

on:
  push:
    branches: [main]
    paths:
      - "openapi/**"
  workflow_dispatch:
    inputs:
      dispatch_tool_router:
        description: Re-dispatch the current generated OpenAPI revision to Tool Router
        required: false
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  sync-openapi:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      docs_commit: ${{ steps.docs_revision.outputs.commit }}
      openapi_sha256: ${{ steps.docs_revision.outputs.openapi_sha256 }}
      docs_changed: ${{ steps.docs_diff.outputs.changed }}

    steps:
      # ── 1. Checkout docs repo ──────────────────────────────────────
      - name: Checkout docs repo
        uses: actions/checkout@v4

      # ── 2. Set up Python ───────────────────────────────────────────
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install pyyaml

      # ── 3. Generate consolidated spec ──────────────────────────────
      - name: Consolidate OpenAPI specs
        id: consolidate
        run: |
          python scripts/consolidate_openapi.py --output /tmp/openapi.yaml 2>&1 | tee /tmp/consolidate.log
          STATS=$(head -1 /tmp/consolidate.log | tr -d '\n')
          echo "stats=$STATS" >> "$GITHUB_OUTPUT"

      - name: Validate output
        run: |
          python -c "
          import yaml, sys
          with open('/tmp/openapi.yaml') as f:
              spec = yaml.safe_load(f)
          paths = len(spec['paths'])
          schemas = len(spec['components']['schemas'])
          print(f'Valid OpenAPI {spec[\"openapi\"]} — {paths} paths, {schemas} schemas')
          if paths < 50:
              print('ERROR: Too few paths, something went wrong')
              sys.exit(1)
          "

      # ── 4. Commit spec back to this repo (public download URL) ─────
      - name: Copy spec to docs repo root
        run: cp /tmp/openapi.yaml openapi.yaml

      - name: Check for docs-repo changes
        id: docs_diff
        run: |
          git diff --quiet openapi.yaml && echo "changed=false" >> "$GITHUB_OUTPUT" || echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Commit and push to docs repo
        if: steps.docs_diff.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add openapi.yaml
          git commit -m "chore: regenerate consolidated openapi.yaml

          Auto-generated by sync-openapi workflow.
          Triggered by: ${{ github.event_name }} on ${{ github.sha }}"
          git push

      - name: Capture published docs revision
        id: docs_revision
        run: |
          echo "commit=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
          echo "openapi_sha256=$(sha256sum /tmp/openapi.yaml | cut -d ' ' -f 1)" >> "$GITHUB_OUTPUT"

      - name: Summary
        run: |
          if [ "${{ steps.docs_diff.outputs.changed }}" = "true" ]; then
            echo "✅ openapi.yaml updated and pushed to AIsa-team/docs"
          else
            echo "ℹ️ docs/openapi.yaml already up to date"
          fi

      # ── 5. Slack notifications ─────────────────────────────────────
      - name: Notify Slack (spec updated)
        if: steps.docs_diff.outputs.changed == 'true'
        uses: slackapi/slack-github-action@v2.1.0
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: |
            {
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "✅ OpenAPI Spec Updated",
                    "emoji": true
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "The consolidated *openapi.yaml* has been regenerated.\n\n• *Trigger:* `${{ github.event_name }}`\n• *Published commit:* <https://github.com/${{ github.repository }}/commit/${{ steps.docs_revision.outputs.commit }}|`${{ steps.docs_revision.outputs.commit }}`>\n• *Stats:* ${{ steps.consolidate.outputs.stats }}\n• *Docs repo:* `${{ steps.docs_diff.outputs.changed == 'true' && 'pushed' || 'no change' }}`\n• *Workflow run:* <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View logs>"
                  }
                }
              ]
            }

      - name: Notify Slack (no changes)
        if: steps.docs_diff.outputs.changed == 'false'
        uses: slackapi/slack-github-action@v2.1.0
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: |
            {
              "text": "ℹ️ OpenAPI sync ran — no changes detected. Spec is already up to date."
            }

      - name: Notify Slack (failure)
        if: failure()
        uses: slackapi/slack-github-action@v2.1.0
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: |
            {
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "⚠️ OpenAPI Sync Failed",
                    "emoji": true
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "The sync-openapi workflow failed.\n\n• *Workflow run:* <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View logs>"
                  }
                }
              ]
            }

  dispatch-tool-router:
    needs: sync-openapi
    # The docs outputs are captured before notification steps. `always()` lets
    # this consumer proceed when a later notification fails; a blank commit means
    # docs publication itself never reached the handoff boundary.
    if: >-
      always() &&
      github.ref == 'refs/heads/main' &&
      needs.sync-openapi.outputs.docs_commit != '' &&
      (needs.sync-openapi.outputs.docs_changed == 'true' || inputs.dispatch_tool_router)
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Generate Tool Router App token
        id: tool_router_app_token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ vars.TOOL_ROUTER_DISPATCH_APP_ID }}
          private-key: ${{ secrets.TOOL_ROUTER_DISPATCH_APP_PRIVATE_KEY }}
          owner: AIsa-team
          repositories: aisa-tool-router

      - name: Dispatch OpenAPI update to Tool Router
        env:
          GH_TOKEN: ${{ steps.tool_router_app_token.outputs.token }}
          DOCS_COMMIT: ${{ needs.sync-openapi.outputs.docs_commit }}
          OPENAPI_SHA256: ${{ needs.sync-openapi.outputs.openapi_sha256 }}
        run: |
          if [ -z "$GH_TOKEN" ]; then
            echo "::error::aisa-release-bot did not issue an installation token"
            exit 1
          fi
          jq -n \
            --arg event_type "docs_openapi_updated" \
            --arg docs_repository "${{ github.repository }}" \
            --arg docs_commit "$DOCS_COMMIT" \
            --arg openapi_sha256 "$OPENAPI_SHA256" \
            --arg source_commit "${{ github.sha }}" \
            --arg source_ref "${{ github.ref_name }}" \
            --arg workflow_run_id "${{ github.run_id }}" \
            '{
              event_type: $event_type,
              client_payload: {
                docs_repository: $docs_repository,
                docs_commit: $docs_commit,
                openapi_sha256: $openapi_sha256,
                source_commit: $source_commit,
                source_ref: $source_ref,
                workflow_run_id: $workflow_run_id
              }
            }' > /tmp/tool-router-dispatch.json
          gh api \
            --method POST \
            repos/AIsa-team/aisa-tool-router/dispatches \
            --input /tmp/tool-router-dispatch.json
