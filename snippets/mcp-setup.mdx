{/*
  One source for the sentence that would otherwise be repeated on every API
  reference page. Included, not copied: the add commands and the wording have
  already changed once, and a paragraph pasted into hundreds of pages is a
  paragraph that goes stale in hundreds of places.

  No ?from= here, though the generated line below this note carries one.
  Measured on 2026-09-16: Mintlify substitutes a snippet prop in markdown text
  and nowhere else — inside a link destination, an autolinked URL, inline code
  or a JSX expression it stays literal, so the href came out as
  ?from=%%7Bpage%%7D. The per-page link is that generated line's job; this note
  is the generic one.

  The #connect fragment is static, so nothing has to substitute it. The reader
  lands on the section that lists the client commands instead of the top of a
  long page — the one thing the missing referrer used to cost us here. What it
  still cannot do is say which endpoint the reader came from; that stays with
  the generated line.
*/}

<Note>
  **First time?** Point any MCP client at `https://mcp.aisa.one/mcp` — Claude
  Code, Codex, Cursor, VS Code and the rest. Authorization is OAuth: the client
  opens a browser, you click Allow once, and there is no key to paste. The
  commands per client, and what each call costs, are on
  [aisa.one/mcp](https://aisa.one/mcp#connect).
</Note>
