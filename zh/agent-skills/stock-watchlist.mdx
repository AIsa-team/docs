---
title: "自选列表管理"
description: "用目标位、止损位和提醒检查管理 ticker 自选列表。"
---

[在 GitHub 上查看 ->](https://github.com/AIsa-team/agent-skills/tree/main/stock-watchlist)

**带实时提醒检查的自选列表。** 使用 AIsa 市场数据，按目标位、止损位和信号变化跟踪 ticker。

## 安装

如果还没有安装 AIsa CLI，请先安装：

```bash
npm install -g @aisa-one/cli
```

然后安装该技能：

```bash
aisa skills install stock-watchlist
```

## Agent 可以用它做什么？

<CardGroup cols={2}>
  <Card title="自选列表跟踪" icon="list-check">
    维护 symbol、投资逻辑备注和后续操作。
  </Card>
  <Card title="目标位检查" icon="bullseye">
    检查价格是否到达目标水平。
  </Card>
  <Card title="止损监控" icon="shield-alert">
    标记需要关注的下行价位。
  </Card>
  <Card title="提醒摘要" icon="bell">
    总结自选列表变化，便于复核。
  </Card>
</CardGroup>

## 用法

```bash
# 添加带目标价和止损价的 ticker
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-watchlist/scripts/watchlist.py" add AAPL --target 220 --stop 160

# 添加信号变化提醒
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-watchlist/scripts/watchlist.py" add AAPL --alert-on signal

# 列出所有自选项
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-watchlist/scripts/watchlist.py" list

# 检查实时价格并触发提醒
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-watchlist/scripts/watchlist.py" check

# 检查并发送通知
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-watchlist/scripts/watchlist.py" check --notify

# 删除 ticker
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-watchlist/scripts/watchlist.py" remove AAPL
```

### 操作

| 操作 | 说明 |
|--------|-------------|
| `add TICKER` | 添加 ticker，可选 `--target`、`--stop`、`--alert-on signal` |
| `remove TICKER` | 从自选列表删除 ticker |
| `list` | 显示所有自选项 |
| `check` | 获取实时价格并检查提醒 |

## 数据存储

自选列表数据存储在 `${CLAUDE_PLUGIN_DATA}/watchlist.json`，可跨会话持久化。

**非投资建议。** 仅供参考。

## 开始使用

1. 在 [aisa.one](https://aisa.one) 注册（新账户有 \$2 免费额度）。
2. 从控制台生成 API key。
3. 设置 key 并安装技能：
   ```bash
   export AISA_API_KEY="your-key"
   npm install -g @aisa-one/cli
   aisa skills install stock-watchlist
   ```
4. 启动新的 Agent 会话，让运行时加载更新后的技能说明。

## 相关

<CardGroup cols={3}>
  <Card title="股票与加密资产分析" icon="file-chart-line" href="/zh/agent-skills/stock-analysis">
    添加前先分析资产。
  </Card>
  <Card title="投资组合管理" icon="wallet" href="/zh/agent-skills/stock-portfolio">
    复核持仓和配置。
  </Card>
  <Card title="热点扫描器" icon="fire" href="/zh/agent-skills/stock-hot">
    发现新的自选候选。
  </Card>
</CardGroup>
