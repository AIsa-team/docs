---
title: "Agent Skills Quickstart"
description: "安装 AIsa CLI，使用你的 API key 完成认证，并把可复用 agent skills 添加到 Claude Code、Cursor、Codex、OpenClaw 和其他支持的 agents。"
privacy:
  view: "public"
---

本指南会带你在大约五分钟内，从零开始安装并使用一个 skill。

## Prerequisites

- Node.js 18 或更高版本
- AIsa 账户和 API key — [在 aisa.one 注册](https://console.aisa.one/)（新账户包含 \$2 free credit）
- 至少安装一个受支持的 AI coding agent：Claude Code、Cursor、GitHub Copilot、Windsurf、Codex、Gemini CLI 或 OpenClaw

---

## Step 1: 安装 AIsa CLI

```bash
npm install -g @aisa-one/cli
```

验证是否可用：

```bash
aisa --version
```

---

## Step 2: 认证

```bash
aisa login --key YOUR_AISA_API_KEY
```

也可以设置环境变量——它的优先级高于已存储 key：

```bash
export AISA_API_KEY="YOUR_AISA_API_KEY"
```

随时检查认证状态：

```bash
aisa whoami
```

---

## Step 3: 浏览可用 skills

```bash
aisa skills list
```

该命令会从 [agent-skills](https://github.com/AIsa-team/agent-skills) registry 拉取 live catalogue，并打印每个 skill 的 name、slug 和 description。

按关键词搜索：

```bash
aisa skills search "finance"
aisa skills search "search"
```

安装前查看某个 skill 的完整详情：

```bash
aisa skills show market
```

---

## Step 4: 安装 skill

```bash
aisa skills install search
```

CLI 会自动检测你机器上每个受支持的 agent，并把 skill 写入各自的 skills directory。你会看到每个 agent 对应的一行确认信息：

```text
✓ ~/.claude/skills/ (claude)
✓ ~/.cursor/skills/ (cursor)
Skill 'search' installed to 2 agent(s)
```

如果只想为特定 agent 安装：

```bash
aisa skills install search --agent claude
```

如果想无视自动检测结果，为所有支持的 agents 安装：

```bash
aisa skills install search --agent all
```

---

## Step 5: 使用 skill

在你的 agent 中打开一个**新 session**（skills 会在 session 启动时加载，而不是在 session 中途加载）。然后让它执行该 skill 能处理的任务：

```text
Search the web for the latest news on AI agent frameworks and summarise the top three stories.
```

Agent 会读取 `search` skill description，加载完整 `SKILL.md` instructions，并使用你的 key 调用 AIsa search API。

---

## Step 6: 安装多个 skills

```bash
aisa skills install market
aisa skills install twitter
aisa skills install media-gen
```

安装多个 skills 后，agent 可以根据具体任务自动选择合适的 skill——不需要你指定使用哪一个。

---

## 管理已安装 skills

```bash
# List skills available in the registry
aisa skills list

# Remove a skill from all agent directories
aisa skills remove market

# Remove from a specific agent only
aisa skills remove market --agent cursor
```

---

## 创建自己的 skill

从模板 scaffold 一个新 skill：

```bash
# Default blank template
aisa skills init my-skill

# Start from a specific template
aisa skills init my-skill --template finance    # finance template
aisa skills init my-skill --template search     # search template
aisa skills init my-skill --template llm        # LLM gateway template
aisa skills init my-skill --template twitter    # Twitter template
aisa skills init my-skill --template video      # video generation template
```

这会创建一个 `my-skill/` folder，其中包含预填充的 `SKILL.md`。编辑 `name`、`description` 和正文，描述你的 capability。然后像安装其他 skill 一样安装它：

```bash
aisa skills install ./my-skill    # install from local path
```

如需与社区分享你的 skill，请向 [AIsa-team/agent-skills](https://github.com/AIsa-team/agent-skills) 提交 pull request。

---

## 下一步

- [Agent Skills](/zh/agent-skills) — 包含 descriptions 和 GitHub links 的完整 catalogue
- [Standards](/zh/agent-skills/standards) — 了解 SKILL.md 文件如何工作，以及如何编写自己的 skill
- [Agent Skills vs custom skills](/zh/guides/learn/agent-skills-vs-tools) — 什么时候使用 registry skill，什么时候构建自己的 skill
- 有问题？[联系我们](mailto:developer@aisa.one)
