---
title: "投资组合管理"
description: "跟踪持仓、配置和实时 P&L。"
---

[在 GitHub 上查看 ->](https://github.com/AIsa-team/agent-skills/tree/main/stock-portfolio)

**面向 Agent 的投资组合跟踪。** 使用 AIsa 市场数据管理持仓、配置和实时 P&L 快照。

## 安装

如果还没有安装 AIsa CLI，请先安装：

```bash
npm install -g @aisa-one/cli
```

然后安装该技能：

```bash
aisa skills install stock-portfolio
```

## Agent 可以用它做什么？

<CardGroup cols={2}>
  <Card title="持仓跟踪" icon="wallet">
    总结投资组合持仓和风险敞口。
  </Card>
  <Card title="配置复核" icon="chart-pie">
    按资产、行业或主题检查集中度。
  </Card>
  <Card title="实时 P&L" icon="chart-line">
    使用市场数据估算当前盈亏。
  </Card>
  <Card title="再平衡备注" icon="scale-balanced">
    标记需要复核的持仓。
  </Card>
</CardGroup>

## 用法

```bash
# 创建新组合
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-portfolio/scripts/portfolio.py" create "My Portfolio"

# 添加持仓
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-portfolio/scripts/portfolio.py" add AAPL --quantity 10 --cost 150
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-portfolio/scripts/portfolio.py" add BTC-USD --quantity 0.5 --cost 40000

# 展示带实时 P&L 的组合
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-portfolio/scripts/portfolio.py" show
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-portfolio/scripts/portfolio.py" show --portfolio "My Portfolio"

# 更新持仓
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-portfolio/scripts/portfolio.py" update AAPL --quantity 15 --cost 160

# 删除持仓
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-portfolio/scripts/portfolio.py" remove AAPL

# 列出所有组合
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-portfolio/scripts/portfolio.py" list

# 重命名组合
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-portfolio/scripts/portfolio.py" rename "My Portfolio" "Tech Holdings"

# 删除组合
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-portfolio/scripts/portfolio.py" delete "Old Portfolio"
```

### 操作

| Action | Description |
|--------|-------------|
| `create NAME` | 创建新投资组合 |
| `list` | 列出所有投资组合 |
| `show` | 展示带实时 P&L 的组合摘要 |
| `add TICKER` | 使用 `--quantity` 和 `--cost` 添加持仓 |
| `update TICKER` | 更新持仓数量/成本 |
| `remove TICKER` | 从组合中删除持仓 |
| `rename OLD NEW` | 重命名投资组合 |
| `delete NAME` | 删除投资组合 |

## 数据存储

组合数据存储在 `${CLAUDE_PLUGIN_DATA}/portfolios.json`，可跨会话持久化。

**非投资建议。** 仅供参考。

## 开始使用

1. 在 [aisa.one](https://aisa.one) 注册（新账户有 $2 免费额度）。
2. 从控制台生成 API key。
3. 设置 key 并安装技能：
   ```bash
   export AISA_API_KEY="your-key"
   npm install -g @aisa-one/cli
   aisa skills install stock-portfolio
   ```
4. 启动新的 Agent 会话，让运行时加载更新后的技能说明。

## 相关

<CardGroup cols={3}>
  <Card title="自选列表管理" icon="list-check" href="/zh/agent-skills/stock-watchlist">
    跟踪目标位和提醒检查。
  </Card>
  <Card title="股票与加密资产分析" icon="file-chart-line" href="/zh/agent-skills/stock-analysis">
    分析单个持仓。
  </Card>
  <Card title="AIsa Market" icon="chart-line" href="/agent-skills/market">
    用于组合复核的市场数据。
  </Card>
</CardGroup>
