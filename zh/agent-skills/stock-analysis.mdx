---
title: "股票与加密资产分析"
description: "结合实时市场上下文分析股票和加密资产代码。"
---

[在 GitHub 上查看 ->](https://github.com/AIsa-team/agent-skills/tree/main/stock-analysis)

**使用实时数据进行股票和加密资产分析。** 通过 AIsa 分析代码、风险信号、目标价位和市场上下文。

## 安装

如果还没有安装 AIsa CLI，请先安装：

```bash
npm install -g @aisa-one/cli
```

然后安装该技能：

```bash
aisa skills install stock-analysis
```

## Agent 可以用它做什么？

<CardGroup cols={2}>
  <Card title="分析报告" icon="file-chart-line">
    创建结构化股票或加密资产分析报告。
  </Card>
  <Card title="风险标记" icon="triangle-exclamation">
    暴露波动率、流动性和催化剂风险。
  </Card>
  <Card title="价位与目标" icon="bullseye">
    提出待复核的观察价位、目标位和止损位。
  </Card>
  <Card title="评分" icon="gauge-high">
    用可复用评分框架对比机会。
  </Card>
</CardGroup>

## 设置

该技能需要 AIsa API key。可通过插件配置或环境变量设置：

```bash
export AISA_API_KEY="your-key-here"
export AISA_BASE_URL=https://api.aisa.one/v1   # 可选
export AISA_MODEL=gpt-4o                       # 可选
```

也可以使用插件的 `userConfig` 值（启用插件时会自动设置）。

## 用法

用一个或多个 ticker symbol 运行分析脚本：

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-analysis/scripts/analyze_stock.py" AAPL
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-analysis/scripts/analyze_stock.py" BTC-USD ETH-USD
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-analysis/scripts/analyze_stock.py" AAPL MSFT GOOGL
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-analysis/scripts/analyze_stock.py" AAPL --fast
python3 "${CLAUDE_PLUGIN_ROOT}/skills/stock-analysis/scripts/analyze_stock.py" AAPL --output json
```

### 参数

- **Tickers**：一个或多个股票代码（如 `AAPL`、`MSFT`）或加密资产代码（如 `BTC-USD`、`ETH-USD`）
- `--fast`：跳过较慢分析（内幕交易、详细新闻），以更快返回结果
- `--output json`：在分析后追加结构化 JSON 摘要

### 多 ticker 对比

提供多个 ticker 时，脚本会先生成单独分析，然后给出排序对比表：

| Ticker | Score | Signal | Key Strength | Key Risk |
|--------|-------|--------|-------------|----------|

## 8 维评分（股票）

| # | 维度 | 权重 |
|---|-----------|--------|
| 1 | Earnings Surprise | 30% |
| 2 | Fundamentals（P/E、利润率、增长） | 20% |
| 3 | Analyst Sentiment | 20% |
| 4 | Historical Patterns | 10% |
| 5 | Market Context（VIX、SPY/QQQ） | 10% |
| 6 | Sector Performance | 15% |
| 7 | Momentum（RSI、52w range） | 15% |
| 8 | Sentiment（Fear/Greed、shorts、insiders） | 10% |

## 3 维评分（加密资产）

| # | 维度 | 权重 |
|---|-----------|--------|
| 1 | Market Cap & Category | 40% |
| 2 | BTC Correlation（30-day） | 30% |
| 3 | Momentum（RSI、range、volume） | 30% |

## 风险标记

自动检测：Pre-earnings、Post-spike、Overbought、Risk-Off、Breaking News。

## 输出

最终建议包含：**Score（0-10）**、**Signal（BUY/HOLD/SELL）**、**Confidence（High/Medium/Low）**，以及 **Entry / Target / Stop prices**。

**非投资建议。** 仅供参考。

## 开始使用

1. 在 [aisa.one](https://aisa.one) 注册（新账户有 $2 免费额度）。
2. 从控制台生成 API key。
3. 设置 key 并安装技能：
   ```bash
   export AISA_API_KEY="your-key"
   npm install -g @aisa-one/cli
   aisa skills install stock-analysis
   ```
4. 启动新的 Agent 会话，让运行时加载更新后的技能说明。

## 相关

<CardGroup cols={3}>
  <Card title="AIsa Market" icon="chart-line" href="/agent-skills/market">
    更广泛的股票和加密资产市场数据。
  </Card>
  <Card title="自选列表管理" icon="list-check" href="/zh/agent-skills/stock-watchlist">
    跟踪目标位、止损位和提醒检查。
  </Card>
  <Card title="美股分析师" icon="file-invoice-dollar" href="/agent-skills/us-stock-analyst">
    更深入的美股分析报告。
  </Card>
</CardGroup>
