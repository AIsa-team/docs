---
title: "获取用户提及"
description: "获取提及某个 @handle 的推文，每页 20 条。"
openapi: "openapi/zh/twitter-user-batch_02.json GET /twitter/user/mentions"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

获取提及某个 @handle 的推文，每页 20 条。这是唯一支持时间窗口的用户维度 X endpoint：传入 Unix 秒级时间戳 `sinceTime` 和/或 `untilTime` 来限定范围。适合用于监测——品牌提及、收到的回复、声誉跟踪，或「这个账号过去 24 小时发生了什么」。

返回推文并带 `has_next_page` 和 `next_cursor`。

如果要的是该账号自己发布的帖子而不是对它的提及，请使用 [`get_twitter_user_tweet_timeline`](/zh/api-reference/twitter/get_twitter-user-tweet-timeline)。
