---
title: "智能搜索"
description: "同时搜索网页与学术来源，适合跨在两边的问题。"
openapi: "openapi/zh/platform-txyz-openapi.json POST /scholar/search/mixed"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

同时搜索网页与学术来源，适合跨在两边的问题。⚠️ 参数放在 **query string** 里：`query`（必填）、`max_num_results`、`as_ylo`/`as_yhi`。返回一个检索 `id` 和 `results[]`；**条目形状随来源变化** —— 每条都有 `title`、`link`、`snippet`，学术来源的还额外带 `authors` 和 `number_of_citations`，所以把这两个字段当作可选的，不要默认它们存在。实测约 3 秒。当你事先不知道哪类来源能回答时用它。已经知道的话，[`post_scholar_search_web`](/zh/api-reference/scholar/post_scholar-search-web) 或 [`post_scholar_search_scholar`](/zh/api-reference/scholar/post_scholar-search-scholar) 的返回更可预期。记住 `id`，[`post_scholar_search_explain`](/zh/api-reference/scholar/post_scholar-search-explain) 要用。
