---
title: "抖音 uid 转加密 user_id"
description: "抖音 uid 转加密 user_id"
openapi: "openapi/tikhub.json GET /tikhub/douyin/index/fetch_encrypt_user_id"
---

抖音 uid 转加密 user_id

**计费** — 成功调用基准价 $0.00145（= provider 成本 × 1.45；最终扣费按你的套餐倍率浮动）。仅当上游响应体 `code` 为 `200` 时计费。

## 示例

```bash
curl "https://api.aisa.one/apis/v1/tikhub/douyin/index/fetch_encrypt_user_id?uid=VALUE" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
