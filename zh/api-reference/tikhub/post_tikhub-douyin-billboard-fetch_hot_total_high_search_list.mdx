---
title: "获取热度飙升的搜索榜"
description: "获取热度飙升的搜索榜"
openapi: "openapi/tikhub.json POST /tikhub/douyin/billboard/fetch_hot_total_high_search_list"
---

获取热度飙升的搜索榜

**计费** — 成功调用基准价 $0.00145（= provider 成本 × 1.45；最终扣费按你的套餐倍率浮动）。仅当上游响应体 `code` 为 `200` 时计费。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/douyin/billboard/fetch_hot_total_high_search_list" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # 请求体结构见下方 schema
```
