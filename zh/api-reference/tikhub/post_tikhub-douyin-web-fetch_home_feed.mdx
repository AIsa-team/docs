---
title: "获取首页推荐数据"
description: "获取首页推荐数据"
openapi: "openapi/tikhub.json POST /tikhub/douyin/web/fetch_home_feed"
---

获取首页推荐数据

**计费** — 成功调用基准价 $0.00145（= provider 成本 × 1.45；最终扣费按你的套餐倍率浮动）。仅当上游响应体 `code` 为 `200` 时计费。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/douyin/web/fetch_home_feed" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # 请求体结构见下方 schema
```
