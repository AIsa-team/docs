---
title: "生成 X-Mssdk-Info"
description: "生成 X-Mssdk-Info"
openapi: "openapi/tikhub.json POST /tikhub/tiktok/web/generate_x_mssdk_info"
---

生成 X-Mssdk-Info

**计费** — 成功调用基准价 $0.0029（= provider 成本 × 1.45；最终扣费按你的套餐倍率浮动）。仅当上游响应体 `code` 为 `200` 时计费。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/tikhub/tiktok/web/generate_x_mssdk_info" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'  # 请求体结构见下方 schema
```
