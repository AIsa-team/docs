---
title: "获取视频笔记详情"
description: "获取视频笔记详情"
openapi: "openapi/tikhub.json GET /tikhub/xiaohongshu/app_v2/get_video_note_detail"
---

获取视频笔记详情

**计费** — 成功调用基准价 $0.0145（= provider 成本 × 1.45；最终扣费按你的套餐倍率浮动）。仅当上游响应体 `code` 为 `200` 时计费。

## 示例

```bash
curl "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/app_v2/get_video_note_detail" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
