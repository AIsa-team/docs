---
title: "搜索用户"
description: "搜索用户"
openapi: "openapi/tikhub.json GET /tikhub/xiaohongshu/app_v2/search_users"
---

搜索用户

**计费** — 成功调用基准价 $0.0145（= provider 成本 × 1.45；最终扣费按你的套餐倍率浮动）。仅当上游响应体 `code` 为 `200` 时计费。

## 示例

```bash
curl "https://api.aisa.one/apis/v1/tikhub/xiaohongshu/app_v2/search_users?keyword=美食博主" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
