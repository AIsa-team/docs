---
title: "搜索邮件"
description: "在单个收件箱的邮件中做全文检索。"
openapi: "openapi/zh/agentmail.json GET /agentmail/inboxes/{inbox_id}/messages/search"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

在单个收件箱的邮件中做全文检索。返回 `count`、`limit`、`next_page_token` 和 `messages`，用 `next_page_token` 翻页。字段与 `get_agentmail_inbox_messages` 相同，后者用于按时间顺序取全部而非带条件查询。要检索会话而非单封邮件，用 `get_agentmail_inbox_threads_search`。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/agentmail/inboxes/{inbox_id}/messages/search" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
