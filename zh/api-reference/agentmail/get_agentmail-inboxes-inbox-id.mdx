---
title: "获取收件箱"
description: "按 inbox_id 获取单个收件箱，返回 inbox_id、email、display_name、client_id、pod_id、metadata、created_at 和 updated_at——只有设置，没有邮件。"
openapi: "openapi/zh/agentmail.json GET /agentmail/inboxes/{inbox_id}"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

按 `inbox_id` 获取单个收件箱，返回 `inbox_id`、`email`、`display_name`、`client_id`、`pod_id`、`metadata`、`created_at` 和 `updated_at`——只有设置，没有邮件。要邮件用 `get_agentmail_inbox_messages`，要会话用 `get_agentmail_inbox_threads`。id 从 `get_agentmail_inboxes` 获得。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/agentmail/inboxes/{inbox_id}" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
