---
title: "获取草稿附件"
description: "获取未发送草稿上某个附件的元数据。"
openapi: "openapi/zh/agentmail.json GET /agentmail/inboxes/{inbox_id}/drafts/{draft_id}/attachments/{attachment_id}"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

获取未发送草稿上某个附件的元数据。返回 `attachment_id`、`filename`、`size`、`content_type`、`content_disposition`、`content_id`，以及一个短时效的 `download_url` 和 `expires_at`。字节内容不内联，需在过期前去取 `download_url`。id 来自 `get_agentmail_inbox_draft` 返回的 `attachments` 数组。已发送邮件的孪生工具是 `get_agentmail_inbox_message_attachment`。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/agentmail/inboxes/{inbox_id}/drafts/{draft_id}/attachments/{attachment_id}" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
