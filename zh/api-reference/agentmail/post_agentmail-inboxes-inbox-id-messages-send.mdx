---
title: "发送邮件"
description: "从某个收件箱发出一封新邮件，返回 message_id 和 thread_id。"
openapi: "openapi/zh/agentmail.json POST /agentmail/inboxes/{inbox_id}/messages/send"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

从某个收件箱发出一封新邮件，返回 `message_id` 和 `thread_id`。接受 `to`、`cc`、`bcc`、`subject`、`text`、`html`、`labels` 和 `attachments`。🔴 **这会发出真实邮件，且发出去收不回。**所有 AIsa 调用方共用同一个 AgentMail 账号，因此这里会触及其他调用方创建的收件箱，所以发件地址未必是你创建的——先用 `get_agentmail_inbox` 确认。回复既有邮件用 `post_agentmail_inbox_message_reply`；只写不发用 `post_agentmail_inbox_draft`。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/agentmail/inboxes/{inbox_id}/messages/send" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
