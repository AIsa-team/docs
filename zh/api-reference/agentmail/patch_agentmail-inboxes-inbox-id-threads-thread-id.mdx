---
title: "更新会话标签"
description: "增删整条会话的标签，返回 thread_id 和结果 labels，一次作用于会话内每一封邮件。"
openapi: "openapi/zh/agentmail.json PATCH /agentmail/inboxes/{inbox_id}/threads/{thread_id}"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

增删整条会话的标签，返回 `thread_id` 和结果 `labels`，一次作用于会话内每一封邮件。单封邮件的孪生工具是 `patch_agentmail_inbox_message`。写入的是共享 AgentMail 工作区：所有 AIsa 调用方共用同一个 AgentMail 账号，因此这里会触及其他调用方创建的收件箱，你写进去的东西下一个调用方也看得到、改得动。

## 示例

```bash
curl -X PATCH "https://api.aisa.one/apis/v1/agentmail/inboxes/{inbox_id}/threads/{thread_id}" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
