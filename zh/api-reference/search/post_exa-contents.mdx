---
title: "Exa Contents"
description: "为你已经拿到的 URL 取回页面正文和元信息。"
openapi: "openapi/zh/exa.json POST /exa/contents"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

为你已经拿到的 URL 取回页面正文和元信息。`ids` 必填，接收 [`post_exa_search`](/zh/api-reference/search/post_exa-search) 返回的 `id` —— 那本身就是 URL，所以任何 URL 都能用。可开关 `text`、`highlights`、`summary`、`subpages`、`livecrawl`。返回 `results[]`（含 `id`、`title`、`url`、`author`、`text`），外加一个 **`statuses[]` 数组，逐个 URL 给出 `status` 和 `source`** —— 一定要读，取不到的 URL 记在那里而不是抛错。命中缓存时立即返回，未命中则回落到实时抓取。实测 1.2 秒。按次固定计费 \$0.08。要的是整站而不是一份 URL 清单，用 [`post_firecrawl_crawl`](/zh/api-reference/search/post_firecrawl-crawl)。
