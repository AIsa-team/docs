---
title: "轮询爬取任务"
description: "按 jobId 取回 post_firecrawl_crawl 提交的爬取任务。"
openapi: "openapi/zh/firecrawl.json GET /firecrawl/crawl/{jobId}"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

按 `jobId` 取回 [`post_firecrawl_crawl`](/zh/api-reference/search/post_firecrawl-crawl) 提交的爬取任务。返回和提交时相同的信封 —— `id`、`status`、`createdAt`、`completedAt`、`pricing`、`output`、`error`。反复轮询直到 `status` 为 `completed`、`failed` 或 `cancelled`；成功时 `output` 是页面数组，每项含 `markdown` 和 `metadata`。轮询本身又快又便宜，实测不到 1 秒。它只读取任务，不能发起任务。
