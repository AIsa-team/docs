---
title: "Sonar"
description: "提一个问题，拿回一段带网页引用的文字答案，而不是一堆需要自己读的链接。"
openapi: "openapi/zh/perplexity-openapi.json POST /perplexity/sonar"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

提一个问题，拿回一段带网页引用的文字答案，而不是一堆需要自己读的链接。请求体是 OpenAI chat-completions 形状：`model`（必填，`sonar`）和 `messages`。返回 `choices[0].message.content` 作为正文，外加 `citations`（URL 字符串数组）、`search_results[]`（含 `title`、`url`、`snippet`、`date`、`source`）和一个 `usage` 块。实测约 3 秒。按次计费，每次 \$0.012。这是四个 Perplexity 端点里最快最便宜的一个，适合单个事实性问题。问题分多个部分时升级到 [`post_perplexity_sonar_pro`](/zh/api-reference/perplexity/post_perplexity-sonar-pro)，需要一步步推演时用 [`post_perplexity_sonar_reasoning_pro`](/zh/api-reference/perplexity/post_perplexity-sonar-reasoning-pro)。如果你要的是可遍历的结果而不是成段文字，用 [`post_tavily_search`](/zh/api-reference/search/post_tavily-search)；`post_exa_answer` 用语义检索回答同一类问题，按次 \$0.08。
