---
title: "批量关键词难度"
description: "此端点可在一次 API 请求中提供最多 1,000 个关键词的关键词难度指标。"
openapi: "openapi/zh/dataforseo.json POST /dataforseo/dataforseo_labs/google/bulk_keyword_difficulty/live"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

此端点可在一次 API 请求中提供最多 1,000 个关键词的关键词难度指标。关键词难度表示相关关键词进入自然搜索结果前 10 名的相对难度。DataForSEO API 响应中的关键词难度使用 0 到 100 的对数刻度，表示某个关键词进入自然搜索结果前 10 名的可能性。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/dataforseo/dataforseo_labs/google/bulk_keyword_difficulty/live" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '[{"keywords": "...", "location_name": "...", "location_code": "..."}]'
```
