---
title: "实时 OnPage API 内容解析"
description: "立即把任意 url 解析成结构化内容，不需要先爬取——本家族唯一独立可用的工具。"
openapi: "openapi/zh/dataforseo.json POST /dataforseo/on_page/content_parsing/live"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

立即把任意 `url` 解析成结构化内容，不需要先爬取——本家族唯一独立可用的工具。`browser_preset`、`browser_screen_width`、`browser_screen_height`、`custom_user_agent` 控制渲染，`disable_cookie_popup` 去掉同意弹层，`store_raw_html` 保留源码。example.com 实测 1.2 KB、$0.00015。响应包在信封里：数据在 `tasks[0].result`，成败在 `tasks[0].status_code`——请求被拒时 HTTP 仍是 200。 页面已在某次爬取里时改用 `post_dataforseo_on_page_content_parsing`，那个免费。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/dataforseo/on_page/content_parsing/live" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '[{"url": "...", "custom_user_agent": "...", "browser_preset": "..."}]'
```
