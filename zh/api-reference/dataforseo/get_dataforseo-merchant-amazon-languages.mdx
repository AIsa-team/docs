---
title: "Merchant API 的 Amazon 语言列表"
description: "Amazon 系列接受的 27 种语言，字段为 language_name 和 language_code。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/merchant/amazon/languages"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Amazon 系列接受的 27 种语言，字段为 `language_name` 和 `language_code`。实测 2.1 KB。免费：上游成本为 0。**提交 Amazon 任务前先读它。** 这里是 locale 对——`en_US` 配 `English (United States)`、`ar_SA` 配 `Arabic (Saudi Arabia)`——Google 接受的裸 `en` 在这里会被拒。Google 的对应接口是 `get_dataforseo_merchant_google_languages`。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/merchant/amazon/languages" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
