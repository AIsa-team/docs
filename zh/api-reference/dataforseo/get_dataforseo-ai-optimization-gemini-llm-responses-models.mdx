---
title: "用于 LLM Responses 的 Gemini 模型列表"
description: "可用的 Gemini 模型，每项含 model_name、reasoning、web_search_supported 和 task_post_supported。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/ai_optimization/gemini/llm_responses/models"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

可用的 Gemini 模型，每项含 `model_name`、`reasoning`、`web_search_supported` 和 `task_post_supported`。实测 1.5 KB。免费：上游成本为 0。**调用前先读它**——不在列表里的 `model_name` 会以 `status_code` 40501 被拒，而 HTTP 仍是 200；`task_post_supported` 则告诉你排队版本能不能用。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/ai_optimization/gemini/llm_responses/models" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
