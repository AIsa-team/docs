---
title: "按 id 获取 Yahoo 自然搜索 HTML 结果"
description: "按 id 获取 Yahoo 自然搜索 HTML 结果。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/serp/yahoo/organic/task_get/html/{id}"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

按 id 获取 Yahoo 自然搜索 HTML 结果。返回此前提交的 Yahoo 自然搜索任务的 HTML 结果。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/serp/yahoo/organic/task_get/html/{id}" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
