---
title: "OnPage Lighthouse API 的语言列表"
description: "Lighthouse 报告可用的语言，字段为 language_name 和 language_code。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/on_page/lighthouse/languages"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

Lighthouse 报告可用的语言，字段为 `language_name` 和 `language_code`。实测 2.5 KB。免费：上游成本为 0。传给审计端点的 `language_name` 或 `language_code`——它改变的是结论的措辞，不是测量的内容。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/on_page/lighthouse/languages" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
