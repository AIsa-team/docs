---
title: "获取已完成的“针对 URL 的 Bing Ads 关键词建议”任务"
description: "此端点用于提供尚未获取的已完成任务列表。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/keywords_data/bing/keyword_suggestions_for_url/tasks_ready"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

此端点用于提供尚未获取的已完成任务列表。如果使用 Standard 方法且未指定 `postback_url`，则可通过此端点获取所有已完成任务的 `id` 列表。随后，可使用 **‘Task GET’** 端点获取结果。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/keywords_data/bing/keyword_suggestions_for_url/tasks_ready" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
