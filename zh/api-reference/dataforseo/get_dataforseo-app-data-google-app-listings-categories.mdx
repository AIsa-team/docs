---
title: "App Data API 的 Google 应用列表类别"
description: "post_dataforseo_app_google_app_listings_search_live 接受的类目，字段为 category 和 count——49 行，count 是各类目下的商店页数量。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/app_data/google/app_listings/categories"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

`post_dataforseo_app_google_app_listings_search_live` 接受的类目，字段为 `category` 和 `count`——49 行，count 是各类目下的商店页数量。实测 2.4 KB。免费：上游成本为 0。搜索前先读它；那个 count 也顺便告诉你哪些类目值得搜。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/app_data/google/app_listings/categories" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
