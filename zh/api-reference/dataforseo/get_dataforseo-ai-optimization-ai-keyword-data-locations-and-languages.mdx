---
title: "AI Keyword Data API 的位置和语言列表"
description: "AI 关键词端点接受的地区与语言，字段为 location_code、location_name 和嵌套的 available_languages。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/ai_optimization/ai_keyword_data/locations_and_languages"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

AI 关键词端点接受的地区与语言，字段为 `location_code`、`location_name` 和嵌套的 `available_languages`。实测 13.1 KB。免费：上游成本为 0。属参考数据，取一次复用即可。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/ai_optimization/ai_keyword_data/locations_and_languages" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
