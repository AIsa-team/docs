---
title: "设置 Google Shopping Products 任务"
description: "按 keyword 排队一次 Google Shopping 搜索，返回任务 id。"
openapi: "openapi/zh/dataforseo.json POST /dataforseo/merchant/google/products/task_post"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

按 `keyword` 排队一次 Google Shopping 搜索，返回任务 `id`。`location_code`、`language_code`、`se_domain`、`depth` 调整搜索。本家族全程异步：submit 在 `tasks[0].id` 返回任务 `id`，结果就绪后由 fetch 取回。💰 **费用发生在 submit**（实测 $0.001–0.0015），fetch 免费，重复 fetch 也不再收费。 用 `get_dataforseo_merchant_google_products_fetch` 取结果。submit 实测 $0.001。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/dataforseo/merchant/google/products/task_post" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '[{"keyword": "...", "url": "...", "priority": "..."}]'
```
