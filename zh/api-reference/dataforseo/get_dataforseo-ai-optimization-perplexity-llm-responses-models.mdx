---
title: "用于 LLM Responses 的 Perplexity 模型列表"
description: "可用的 Perplexity 模型——sonar、sonar-pro、sonar-reasoning-pro——每项含 model_name、reasoning、web_search_supported 和 task_post_supported。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/ai_optimization/perplexity/llm_responses/models"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

可用的 Perplexity 模型——`sonar`、`sonar-pro`、`sonar-reasoning-pro`——每项含 `model_name`、`reasoning`、`web_search_supported` 和 `task_post_supported`。实测 745 字节，四份模型列表里最小的。免费：上游成本为 0。注意 **Perplexity 没有排队变体**，live 端点是唯一的。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/ai_optimization/perplexity/llm_responses/models" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
