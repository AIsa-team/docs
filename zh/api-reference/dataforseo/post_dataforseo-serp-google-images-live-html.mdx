---
title: "实时 Google 图片 SERP HTML"
description: "实时 Google 图片 SERP HTML。"
openapi: "openapi/zh/dataforseo.json POST /dataforseo/serp/google/images/live/html"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

实时 Google 图片 SERP HTML。实时返回 Google 图片 SERP 结果的原始 HTML。每次 Live SERP API 调用只能包含一个任务。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/dataforseo/serp/google/images/live/html" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '[{"keyword": "...", "location_code": "...", "location_name": "..."}]'
```
