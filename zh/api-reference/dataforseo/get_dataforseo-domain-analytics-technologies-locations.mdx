---
title: "Domain Analytics Technologies API 的位置列表"
description: "技术类端点接受的地区列表，字段为 location_name 和 country_iso_code。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/domain_analytics/technologies/locations"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

技术类端点接受的地区列表，字段为 `location_name` 和 `country_iso_code`。217 行，实测 29.6 KB——对参考数据来说比看上去大，取一次复用，别每次请求都调。免费：上游成本为 0。⚠️ 注意它与 content 家族的 235 个地区**不是同一份目录**，在那边有效的名字在这里不保证有效。语言的对应接口是 `get_dataforseo_domains_tech_languages`。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/domain_analytics/technologies/locations" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
