---
title: "实时 ChatGPT LLM 抓取工具 API HTML"
description: "与 post_dataforseo_ai_chat_gpt_llm_scraper_live 相同的 ChatGPT 答案，但返回原始 HTML 而非解析结果。"
openapi: "openapi/zh/dataforseo.json POST /dataforseo/ai_optimization/chat_gpt/llm_scraper/live/html"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

与 `post_dataforseo_ai_chat_gpt_llm_scraper_live` 相同的 ChatGPT 答案，但返回原始 HTML 而非解析结果。`expand_citations` 展开来源列表。响应包在信封里：数据在 `tasks[0].result`，成败在 `tasks[0].status_code`——请求被拒时 HTTP 仍是 200。 ⚠️ 比 11.3 KB 的解析版大，且 agent 更难用——只有需要解析器丢掉的标记时才取它。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/dataforseo/ai_optimization/chat_gpt/llm_scraper/live/html" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '[{"keyword": "...", "location_name": "...", "location_code": "..."}]'
```
