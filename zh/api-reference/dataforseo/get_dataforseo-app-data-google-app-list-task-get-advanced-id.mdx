---
title: "按 id 获取 Google 应用列表结果"
description: "取回一份 Google Play 榜单：排名应用，含位次、标题、评分、价格。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/app_data/google/app_list/task_get/advanced/{id}"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

取回一份 Google Play 榜单：排名应用，含位次、标题、评分、价格。免费——费用在 submit。响应包在信封里：数据在 `tasks[0].result`，成败在 `tasks[0].status_code`——请求被拒时 HTTP 仍是 200。 🔴 `_fetch_html` 孪生返回原始商店页源码：同一任务实测 1.3 MB，解析版 21 KB，相差 63 倍。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/app_data/google/app_list/task_get/advanced/{id}" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
