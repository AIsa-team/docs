---
title: "设置 Google Events SERP 任务"
description: "设置 Google Events SERP 任务。"
openapi: "openapi/zh/dataforseo.json POST /dataforseo/serp/google/events/task_post"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

设置 Google Events SERP 任务。仅在设置任务时对您的账户收费。每次 POST 调用最多可发送 100 个任务，稍后可通过任务 id 或 postback/pingback 获取已完成的结果。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/dataforseo/serp/google/events/task_post" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '[{"keyword": "...", "location_code": "...", "location_name": "..."}]'
```
