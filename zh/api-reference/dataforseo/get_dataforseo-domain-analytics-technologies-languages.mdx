---
title: "Domain Analytics Technologies API 的语言列表"
description: "技术类端点接受的语言列表，字段为 language_name 和 language_code。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/domain_analytics/technologies/languages"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

技术类端点接受的语言列表，字段为 `language_name` 和 `language_code`。127 行，实测 6.8 KB。免费：上游成本为 0。属参考数据，取一次复用即可。⚠️ 注意它与 content 家族的 71 种语言**不是同一份目录**，在那边有效的 code 在这里不保证有效。地区的对应接口是 `get_dataforseo_domains_tech_locations`。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/domain_analytics/technologies/languages" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
