---
title: "OnPage API 瀑布图"
description: "某个已爬取 url 的加载时间线——请求了什么、按什么顺序、各花多久。"
openapi: "openapi/zh/dataforseo.json POST /dataforseo/on_page/waterfall"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

某个已爬取 `url` 的加载时间线——请求了什么、按什么顺序、各花多久。实测 835 字节。读的是一次已完成的爬取，因此需要 `post_dataforseo_on_page_submit` 返回的 `id`，并会带回 `crawl_progress` 和 `crawl_status`（`max_crawl_pages`、`pages_in_queue`、`pages_crawled`）——结果偏少时先看这几个字段，爬取还在跑就是本来没那么多。 用它解释页面为什么慢；要带评分的性能审计改用 `post_dataforseo_on_page_lighthouse_live_json`。💰 上游免费：查询一次已完成的爬取不花钱，只有爬取本身花钱。本家族是整个 provider 里最便宜的。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/dataforseo/on_page/waterfall" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '[{"id": "...", "url": "...", "tag": "..."}]'
```
