---
title: "获取已完成的 Google My Business Info 任务"
description: "‘Tasks Ready’ 端点用于提供尚未获取的已完成任务列表。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/business_data/google/my_business_info/tasks_ready"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

**‘Tasks Ready’** 端点用于提供尚未获取的已完成任务列表。如果不使用 `postback_url`，可以通过此端点获取所有已完成任务的 `id` 列表。然后，可以使用 **‘Task GET’** 端点获取结果。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/business_data/google/my_business_info/tasks_ready" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
