---
title: "设置实时“点击流全球搜索量”任务"
description: "keywords 的全球点击流搜索量，完全不带地区参数——给全球总量和分国家拆分。"
openapi: "openapi/zh/dataforseo.json POST /dataforseo/keywords_data/clickstream_data/global_search_volume/live"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

`keywords` 的全球点击流搜索量，完全不带地区参数——给全球总量和分国家拆分。实测 1.9 KB。🔴 **实测上游 $0.18，我们收 $0.012——十五倍，本 provider 里差距最大的一档。**一次调用喂多个关键词，绝不要一个词调一次。 响应包在信封里：数据在 `tasks[0].result`，成败在 `tasks[0].status_code`——请求被拒时 HTTP 仍是 200。 用它决定哪些市场值得再花一次按地区的调用；按地区的版本是 `post_dataforseo_keywords_clickstream_search_volume_live`。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/dataforseo/keywords_data/clickstream_data/global_search_volume/live" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '[{"keywords": "...", "tag": "..."}]'
```
