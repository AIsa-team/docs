---
title: "按 id 获取 Google Ai Mode HTML 结果"
description: "按任务 id 获取 Google AI Mode HTML 结果。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/serp/google/ai_mode/task_get/html/{id}"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

按任务 id 获取 Google AI Mode HTML 结果。AI Mode 目录和已完成任务端点列表中引用了 task-get HTML 端点；路径占位符已从 $id 规范化为 {id}。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/serp/google/ai_mode/task_get/html/{id}" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
