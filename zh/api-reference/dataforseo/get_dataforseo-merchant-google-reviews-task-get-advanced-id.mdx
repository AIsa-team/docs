---
title: "按 id 获取 Google Shopping 评论结果"
description: "取回单个 Google Shopping 商品的买家评价：每条含评分、正文、作者和日期。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/merchant/google/reviews/task_get/advanced/{id}"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

取回单个 Google Shopping 商品的买家评价：每条含评分、正文、作者和日期。免费——费用在 submit。响应包在信封里：数据在 `tasks[0].result`，成败在 `tasks[0].status_code`——请求被拒时 HTTP 仍是 200。 要商品本身的规格和商家用 `get_dataforseo_merchant_google_product_info_fetch`。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/merchant/google/reviews/task_get/advanced/{id}" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
