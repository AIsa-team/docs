---
title: "域名分析 Technologies API 的技术列表"
description: "完整的技术分类树：groups，每个含 id、title 和 categories，每个 category 下再挂技术。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/domain_analytics/technologies/technologies"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

完整的技术分类树：`groups`，每个含 `id`、`title` 和 `categories`，每个 category 下再挂技术。这些正是搜索类端点里 `groups`、`categories`、`technologies` 能接受的取值。🔴 **实测 130 KB，且不接受任何 limit 或过滤参数**——本家族最大的响应，会明显吃掉 agent 的上下文。取一次、留下你需要的那一枝。免费：上游成本为 0。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/domain_analytics/technologies/technologies" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
