---
title: "设置 LLM Responses Claude 任务"
description: "把 Claude 的 prompt 排队而不是等回复，返回任务 id。"
openapi: "openapi/zh/dataforseo.json POST /dataforseo/ai_optimization/claude/llm_responses/task_post"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

把 Claude 的 prompt 排队而不是等回复，返回任务 `id`。响应包在信封里：数据在 `tasks[0].result`，成败在 `tasks[0].status_code`——请求被拒时 HTTP 仍是 200。 先看 `get_dataforseo_ai_claude_llm_responses_models` 里的 `task_post_supported`，再用 `get_dataforseo_ai_claude_llm_responses_fetch` 取结果。

## 示例

```bash
curl -X POST "https://api.aisa.one/apis/v1/dataforseo/ai_optimization/claude/llm_responses/task_post" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '[{"user_prompt": "...", "model_name": "...", "max_output_tokens": "..."}]'
```
