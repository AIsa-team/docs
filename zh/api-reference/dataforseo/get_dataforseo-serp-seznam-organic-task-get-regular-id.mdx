---
title: "按 id 获取 Seznam 自然 SERP 结果（常规）"
description: "按 id 获取 Seznam Organic SERP 结果（常规）。"
openapi: "openapi/zh/dataforseo.json GET /dataforseo/serp/seznam/organic/task_get/regular/{id}"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

按 id 获取 Seznam Organic SERP 结果（常规）。返回先前提交的 Seznam Organic 任务的常规 SERP 结果。

## 示例

```bash
curl -X GET "https://api.aisa.one/apis/v1/dataforseo/serp/seznam/organic/task_get/regular/{id}" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
