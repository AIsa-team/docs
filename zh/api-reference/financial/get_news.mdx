---
title: "公司新闻"
description: "某只股票的近期新闻标题——标题、来源、日期和链接。"
openapi: "openapi/zh/openapi-financial.json GET /news"
---

{/* 正文与 openapi spec 的 operation description 同步维护：改一处请同步另一处 */}

某只股票的近期新闻标题：`title`、`source`、`date`、`url` 以及回显的 `ticker`，放在 `news` 数组中。`ticker` 和 `limit` 是仅有的两个参数——没有全文检索，也没有日期过滤，因此请通过调高或调低 `limit` 来控制范围，而不是靠查询词。适合用于查看你已经确定的某家公司的近期报道。仅返回标题：不含正文，正文请沿 `url` 打开。如需公司自己的申报文件而不是媒体报道，请使用 [`get_financial_filings`](/zh/api-reference/financial/get_filings)。
